<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>在 Linux 系统中实现 sleep 命令</title>
    <url>/2025/11/18/%E5%9C%A8%20Linux%20%E7%B3%BB%E7%BB%9F%E4%B8%AD%E5%AE%9E%E7%8E%B0%20sleep%20%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="用c语言实现自己的-sleep-命令"><a href="#用c语言实现自己的-sleep-命令" class="headerlink" title="用c语言实现自己的 sleep 命令"></a>用c语言实现自己的 sleep 命令</h2><h3 id="写在前面："><a href="#写在前面：" class="headerlink" title="写在前面："></a>写在前面：</h3><p>sleep是用于控制进程暂停的常用命令，本次我将使用系统调用函数，实现一个简单的sleep命令。</p>
<h3 id="功能："><a href="#功能：" class="headerlink" title="功能："></a>功能：</h3><ol>
<li>支持传入多个参数。</li>
<li>对于没有参数或这错误参数的情况，抛出错误。</li>
</ol>
<h3 id="基本思路："><a href="#基本思路：" class="headerlink" title="基本思路："></a>基本思路：</h3><ol>
<li>对于没有参数的情况，我直接抛出错误。</li>
<li>在处理参数的时候，我这里封装在了一个Howtime()的函数内部，可以使得处理参数更加方便，而由于参数是字符串形式传入，所以需要转换为整数形式。</li>
<li>面对负数，非阿拉伯数字等其他字符的时候，我选择抛出错误。</li>
<li>最后把所有时间加和，sleep。</li>
</ol>
<h3 id="源代码："><a href="#源代码：" class="headerlink" title="源代码："></a>源代码：</h3><p>‵‵&#96;<br>#include &lt;stdio.h&gt;&#x2F;&#x2F;fprintf()<br>#include &lt;unistd.h&gt;&#x2F;&#x2F;sleep()<br>#include &lt;stdlib.h&gt;&#x2F;&#x2F;exit()</p>
<p>int Howtime(char* string);</p>
<p>int main(int argc, char* argv[]) {<br>  int total_time &#x3D; 0;<br>  if(argc &#x3D;&#x3D; 1) {<br>    fprintf(stderr, “sleep: argument…\n”);<br>    exit(1);<br>  }</p>
<p>  for(int i &#x3D; 1; i &lt; argc; i++) {<br>    int tmp &#x3D; Howtime(argv[i]);<br>    if(tmp &lt; 0) {<br>      fprintf(stderr, “sleep:invalid time ‘%s’\n”,argv[i]);<br>    }else {<br>      total_time +&#x3D; tmp;<br>    }<br>  }<br>  sleep(total_time);</p>
<p>  exit(0);<br>}</p>
<p>int Howtime(char* string) {<br>  if(string[0] &#x3D;&#x3D; ‘-‘ || string[0] &#x3D;&#x3D; ‘\0’) {<br>    return -1;<br>  }<br>  int time &#x3D; 0;<br>  int i &#x3D; 0;<br>  while(string[i] !&#x3D; ‘\0’) {<br>    if(string[i] &lt; ‘0’ &amp;&amp; string[i] &gt; ‘9’) {<br>      return -1;<br>    }<br>    time &#x3D; time * 10 + (string[i] - ‘0’);<br>    i++;<br>  }<br>  return time;<br>}</p>
<pre><code>
</code></pre>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>在 Linux 系统中实现 ls 命令</title>
    <url>/2025/11/17/%E5%9C%A8%20Linux%20%E7%B3%BB%E7%BB%9F%E4%B8%AD%E5%AE%9E%E7%8E%B0%20ls%20%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="用c语言实现自己的-ls-命令"><a href="#用c语言实现自己的-ls-命令" class="headerlink" title="用c语言实现自己的 ls 命令"></a>用c语言实现自己的 ls 命令</h2><h3 id="写在前面："><a href="#写在前面：" class="headerlink" title="写在前面："></a>写在前面：</h3><p>ls是linux系统编程常用的命令行，可以实现对一个目录的展示，也可以加入不同的参数(l,a,R,……)，满足不同需求，下面是一个使用c语言编写的ls命令</p>
<h3 id="功能："><a href="#功能：" class="headerlink" title="功能："></a>功能：</h3><ol>
<li>实现了 ls 的 -a、-l、-R、-t、-r、-i、-s 参数，并允许这些参数任意组合。</li>
<li><code>-R</code> 可以对 <code>/</code> 遍历测试。</li>
<li>界面美观（输出对齐、带颜色显示等）。</li>
<li>无资源与内存泄漏。</li>
</ol>
<h3 id="基本思路："><a href="#基本思路：" class="headerlink" title="基本思路："></a>基本思路：</h3><ol>
<li>对于参数处理，我使用了宏定义，对每个参数设置了二进制位，全部的参数处理采用位运算，好处是节省空间，便于理解。</li>
<li>对于颜色显示，也使用了宏定义，对不同类型文件的设置不同颜色(此处请用ai搜索标准的颜色显示范例)，好处是便于理解，对于后期如果有需要改动的地方也很好改。</li>
<li>对于输出对齐，起初并没有思路，打算将目录下的文件放入一个自己手动malloc的堆内存里，但直到scandir()出现了。之后只需要获取到当前目录下的文件的最大字符宽度，根据该字符宽度占终端的实时宽度的比值，控制在第几次设置<code>\n</code>。</li>
<li>对于资源与内存泄漏：我直接使用了堆内存。</li>
<li>考虑完上述问题，剩下来的就是把这个目录展示出来，考虑到用户不会传递某个目录参数，所以对于没有目录参数的时候，需要展示当前目录下的文件，这里我使用了isfastoutput()函数。对于有目标目录的参数，需要获取到当前目录的路径，否则之后处理的时候都只是一个文件名，没有对应索引。这里使用了系统调用函数lstat()。而不使用stat()，使用lstat()的主要原因在于处理递归展示的时候，如果不使用lstat()，则会保存当前链接文件的指向，会陷入无限递归。而是用lstat()会剔除这些链接文件。</li>
<li>对于 <code>-R</code> 的实现：我在这里直接在listFiles()函数的尾部进行递归，并在每次递归中，分配一个适合大小的堆内存存储对应的文件，之后在释放文件。</li>
</ol>
<h3 id="源代码："><a href="#源代码：" class="headerlink" title="源代码："></a>源代码：</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include &lt;dirent.h&gt;</span><br><span class="line">#include &lt;stdio.h&gt;</span><br><span class="line">#include &lt;stdlib.h&gt;</span><br><span class="line">#include &lt;string.h&gt;</span><br><span class="line">#include &lt;fcntl.h&gt;</span><br><span class="line">#include &lt;grp.h&gt;</span><br><span class="line">#include &lt;pwd.h&gt;</span><br><span class="line">#include &lt;sys/stat.h&gt;</span><br><span class="line">#include &lt;sys/ioctl.h&gt;</span><br><span class="line">#include &lt;unistd.h&gt;</span><br><span class="line">#include &lt;time.h&gt;</span><br><span class="line">#include &lt;inttypes.h&gt;</span><br><span class="line"></span><br><span class="line">// 掩码确定参数 </span><br><span class="line">#define Ca              0b1               // 显示隐藏文件排列</span><br><span class="line">#define Cl              0b10              // 详细排列</span><br><span class="line">#define CR              0b100             // 递归排列    </span><br><span class="line">#define Ct              0b1000            // 按照最新一次修改时间降序排列</span><br><span class="line">#define Cr              0b10000           // 逆序排列</span><br><span class="line">#define Ci              0b100000          // 显示inode编号排列</span><br><span class="line">#define Cs              0b1000000         // 显示已用内存块数量排列</span><br><span class="line"></span><br><span class="line">// 确定颜色</span><br><span class="line">#define COLOR_RESET     &quot;\033[0m&quot;</span><br><span class="line">#define COLOR_DIR       &quot;\033[1;34m&quot;    // 粗体蓝色</span><br><span class="line">#define COLOR_EXE       &quot;\033[1;32m&quot;    // 粗体绿色</span><br><span class="line">#define COLOR_LINK      &quot;\033[1;36m&quot;    // 粗体青色</span><br><span class="line">#define COLOR_SOCKET    &quot;\033[1;35m&quot;    // 粗体紫色（socket）</span><br><span class="line">#define COLOR_PIPE      &quot;\033[33m&quot;      // 黄色（管道）</span><br><span class="line">#define COLOR_BLOCK     &quot;\033[1;33m&quot;    // 粗体黄色（块设备：u盘，固态)</span><br><span class="line">#define COLOR_CHAR      &quot;\033[1;33m&quot;    // 粗体黄色（字符设备:键鼠）</span><br><span class="line"></span><br><span class="line">void listFiles(const char*, int, int);//根据参数，普通列出目录下的文件</span><br><span class="line">void printWithis(int, struct stat*, int, struct dirent**);//显示</span><br><span class="line">int isfastoutput(int, char*[]);//命令行中传入的参数是否只有该可执行文件</span><br><span class="line">int whatCommand(int, char*[]);//确定参数</span><br><span class="line">int CompareListNormal(const struct dirent**, const struct dirent**);//按照字符顺序排列</span><br><span class="line">int CompareListTime(const struct dirent**, const struct dirent**);//按照最新一次修改时间排列</span><br><span class="line">int HowManyDirpath(int, char*[]);//确定目标路径的数量</span><br><span class="line">int shouldPrintA(int, struct dirent*);//是否需要打印隐藏文件</span><br><span class="line">int maxFileLength(int, struct dirent**, int, int);//确定每个文件的宽度</span><br><span class="line">char* getColor(struct stat);//返回颜色</span><br><span class="line"></span><br><span class="line">int main(int argc, char* argv[]) &#123;</span><br><span class="line">    int tmpargc = HowManyDirpath(argc, argv);</span><br><span class="line">    if(isfastoutput(argc,argv)) &#123;</span><br><span class="line">        int command = whatCommand(argc, argv);</span><br><span class="line">        listFiles(&quot;.&quot;, command, tmpargc);</span><br><span class="line">        printf(&quot;\n&quot;);</span><br><span class="line">    &#125;else &#123;</span><br><span class="line">        int command = whatCommand(argc, argv);</span><br><span class="line">        for(int i = 1; i &lt; argc; i++) &#123;</span><br><span class="line">            listFiles(argv[i], command, tmpargc);</span><br><span class="line">            printf(&quot;\n&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    exit(EXIT_SUCCESS);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">char* getColor(struct stat st) &#123;</span><br><span class="line">    char* color = COLOR_RESET;</span><br><span class="line">    if(S_ISDIR(st.st_mode)) color = COLOR_DIR;</span><br><span class="line">    else if(S_ISLNK(st.st_mode)) color = COLOR_LINK;</span><br><span class="line">    else if(S_ISSOCK(st.st_mode)) color = COLOR_SOCKET;</span><br><span class="line">    else if(S_ISFIFO(st.st_mode)) color = COLOR_PIPE;</span><br><span class="line">    else if(S_ISBLK(st.st_mode) || S_ISCHR(st.st_mode)) color = COLOR_BLOCK;</span><br><span class="line">    else if(st.st_mode &amp; (S_IXUSR | S_IXGRP | S_IXOTH)) color = COLOR_EXE;</span><br><span class="line">    return color;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int CompareListTime(const struct dirent** a, const struct dirent** b) &#123;</span><br><span class="line">    struct stat sta, stb;</span><br><span class="line">    lstat((*a)-&gt;d_name, &amp;sta);lstat((*b)-&gt;d_name, &amp;stb);</span><br><span class="line">    return sta.st_mtime &gt;= stb.st_mtime ? -1 : 1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int isfastoutput(int argc, char* argv[]) &#123;</span><br><span class="line">    int cnt = 0;        </span><br><span class="line">    for(int i = 1; i &lt; argc; i++) if(argv[i][0] == &#x27;-&#x27;) cnt++;</span><br><span class="line">    if(cnt == argc - 1) return 1;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void listFiles(const char* dirpath, int command, int tmpargc) &#123;</span><br><span class="line">    if(dirpath[0] == &#x27;-&#x27;) return;</span><br><span class="line">    if(access(dirpath, F_OK | X_OK) != 0) &#123;</span><br><span class="line">        fprintf(stderr, &quot;无法访问 &#x27;%s&#x27;: 没有那个文件或目录\n&quot;, dirpath);</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line">    int n, maxLength, maxName, Time, enter = 2,fullpath_size = 1024 , term_width = 80;//默认</span><br><span class="line">    long long sum = 0;</span><br><span class="line">    struct dirent** dp;</span><br><span class="line">    struct stat st;</span><br><span class="line">    struct winsize w;</span><br><span class="line">    char* fullpath = (char*)malloc(sizeof(char) * fullpath_size);</span><br><span class="line">    if(command &amp; Ct) n = scandir(dirpath, &amp;dp, NULL, CompareListTime);</span><br><span class="line">    else n = scandir(dirpath, &amp;dp, NULL, CompareListNormal);</span><br><span class="line">    if(n &lt; 0 &amp;&amp; dirpath[0]) &#123;</span><br><span class="line">        perror(&quot;scandir&quot;);</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line">    if(tmpargc &gt; 1) printf(&quot;%s:\n&quot;,dirpath);</span><br><span class="line">    if(command &amp; Cs) &#123;</span><br><span class="line">        for(int i = 0; i &lt; n; ++i) &#123;</span><br><span class="line">            snprintf(fullpath, fullpath_size, &quot;%s/%s&quot;, dirpath, dp[i]-&gt;d_name);</span><br><span class="line">            lstat(fullpath, &amp;st);</span><br><span class="line">            if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">            sum += st.st_blocks / 2;</span><br><span class="line">        &#125;</span><br><span class="line">        printf(&quot;总计 %lld\n&quot;,sum);</span><br><span class="line">    &#125;</span><br><span class="line">    if(!(command &amp; Cl)) &#123;//根据是否需要详细排列，分成两种方案</span><br><span class="line">        if(ioctl(STDOUT_FILENO, TIOCGWINSZ, &amp;w) == 0)term_width = w.ws_col;</span><br><span class="line">        maxLength = maxFileLength(command, dp, n, 0);</span><br><span class="line">        maxName = maxFileLength(command, dp, n, 3);</span><br><span class="line">        Time = term_width / (maxLength + 4);</span><br><span class="line">        Time = Time &lt;= 0 ? 1 : Time;</span><br><span class="line">        for(int i = 0; i &lt; n &amp;&amp; !(command &amp; Cr); i++) &#123;</span><br><span class="line">            snprintf(fullpath, fullpath_size, &quot;%s/%s&quot;, dirpath, dp[i]-&gt;d_name);//将几个字符串以整体的形式送到缓冲区，且函数本身可以防止溢出</span><br><span class="line">            lstat(fullpath, &amp;st);//获取文件详细信息</span><br><span class="line">            if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">            if(enter &gt; Time &amp;&amp; i != 0) printf(&quot;\n&quot;);</span><br><span class="line">            printWithis(command, &amp;st, n, dp);</span><br><span class="line">            if(enter &lt;= Time) &#123;</span><br><span class="line">                printf(&quot;%s%*s%s  &quot;, getColor(st), maxName, dp[i]-&gt;d_name, COLOR_RESET);</span><br><span class="line">                enter++;</span><br><span class="line">            &#125;</span><br><span class="line">            else &#123;</span><br><span class="line">                printf(&quot;%s%*s%s  &quot;, getColor(st), maxName, dp[i]-&gt;d_name, COLOR_RESET);</span><br><span class="line">                enter = 2;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        for(int i = n - 1; i &gt;= 0 &amp;&amp; (command &amp; Cr); i--) &#123;</span><br><span class="line">            snprintf(fullpath, fullpath_size, &quot;%s/%s&quot;, dirpath, dp[i]-&gt;d_name);//将几个字符串以整体的形式送到缓冲区，且函数本身可以防止溢出</span><br><span class="line">            lstat(fullpath, &amp;st);//获取文件详细信息</span><br><span class="line">            if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">            if(enter &gt; Time &amp;&amp; i !=  n - 1) printf(&quot;\n&quot;);</span><br><span class="line">            printWithis(command, &amp;st, n, dp);</span><br><span class="line">            if(enter &lt;= Time) &#123;</span><br><span class="line">                printf(&quot;%s%*s%s  &quot;, getColor(st), maxName, dp[i]-&gt;d_name, COLOR_RESET);</span><br><span class="line">                enter++;</span><br><span class="line">            &#125;</span><br><span class="line">            else &#123;</span><br><span class="line">                printf(&quot;%s%*s%s  &quot;, getColor(st), maxName, dp[i]-&gt;d_name, COLOR_RESET);</span><br><span class="line">                enter = 2;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    else &#123;</span><br><span class="line">        char* tmbuffer = (char*)malloc(sizeof(char) * 80);</span><br><span class="line">        struct stat st;</span><br><span class="line">        struct tm* tm;</span><br><span class="line">        struct passwd* pw;</span><br><span class="line">        struct group* gr;</span><br><span class="line">        char str[11];</span><br><span class="line">        for(int i = 0 ; i &lt; n &amp;&amp; !(command &amp; Cr); i++) &#123;</span><br><span class="line">            snprintf(fullpath, fullpath_size, &quot;%s/%s&quot;, dirpath, dp[i]-&gt;d_name);</span><br><span class="line">            lstat(fullpath, &amp;st);</span><br><span class="line">            if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">            printWithis(command, &amp;st, n, dp);</span><br><span class="line">            str[0] = &#x27;?&#x27;;</span><br><span class="line">            if(S_ISDIR(st.st_mode)) str[0] = &#x27;d&#x27;;</span><br><span class="line">            else if(S_ISLNK(st.st_mode)) str[0] = &#x27;l&#x27;;</span><br><span class="line">            else if(S_ISSOCK(st.st_mode)) str[0] = &#x27;s&#x27;;</span><br><span class="line">            else if(S_ISFIFO(st.st_mode)) str[0] = &#x27;p&#x27;;</span><br><span class="line">            else if(S_ISBLK(st.st_mode)) str[0] = &#x27;b&#x27;;</span><br><span class="line">            else if(S_ISCHR(st.st_mode)) str[0] = &#x27;c&#x27;;</span><br><span class="line">            else str[0] = &#x27;-&#x27;;</span><br><span class="line">            str[1] = (st.st_mode &amp; S_IRUSR) ? &#x27;r&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[2] = (st.st_mode &amp; S_IWUSR) ? &#x27;w&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[3] = (st.st_mode &amp; S_IXUSR) ? &#x27;x&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[4] = (st.st_mode &amp; S_IRGRP) ? &#x27;r&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[5] = (st.st_mode &amp; S_IWGRP) ? &#x27;w&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[6] = (st.st_mode &amp; S_IXGRP) ? &#x27;x&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[7] = (st.st_mode &amp; S_IROTH) ? &#x27;r&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[8] = (st.st_mode &amp; S_IWOTH) ? &#x27;w&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[9] = (st.st_mode &amp; S_IXOTH) ? &#x27;x&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[10] = &#x27;\0&#x27;;</span><br><span class="line">            tm = localtime(&amp;st.st_mtime);</span><br><span class="line">            strftime(tmbuffer, fullpath_size, &quot;%m月 %H:%M&quot;, tm);</span><br><span class="line">            pw = getpwuid(st.st_uid);</span><br><span class="line">            gr = getgrgid(st.st_gid);</span><br><span class="line">            printf(&quot;%s %lu %s %s %ld %s %s%s%s\n&quot;, str, st.st_nlink, pw-&gt;pw_name, gr-&gt;gr_name, st.st_size, tmbuffer, getColor(st), dp[i]-&gt;d_name, COLOR_RESET);</span><br><span class="line">        &#125;</span><br><span class="line">        for(int i = n - 1 ; i &gt;= 0 &amp;&amp; (command &amp; Cr); i--) &#123;</span><br><span class="line">            snprintf(fullpath, fullpath_size, &quot;%s/%s&quot;, dirpath, dp[i]-&gt;d_name);</span><br><span class="line">            lstat(fullpath, &amp;st);</span><br><span class="line">            if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">            printWithis(command, &amp;st, n, dp);</span><br><span class="line">            str[0] = &#x27;?&#x27;;</span><br><span class="line">            if(S_ISDIR(st.st_mode)) str[0] = &#x27;d&#x27;;</span><br><span class="line">            else if(S_ISLNK(st.st_mode)) str[0] = &#x27;l&#x27;;</span><br><span class="line">            else if(S_ISSOCK(st.st_mode)) str[0] = &#x27;s&#x27;;</span><br><span class="line">            else if(S_ISFIFO(st.st_mode)) str[0] = &#x27;p&#x27;;</span><br><span class="line">            else if(S_ISBLK(st.st_mode)) str[0] = &#x27;b&#x27;;</span><br><span class="line">            else if(S_ISCHR(st.st_mode)) str[0] = &#x27;c&#x27;;</span><br><span class="line">            else str[0] = &#x27;-&#x27;;</span><br><span class="line">            str[1] = (st.st_mode &amp; S_IRUSR) ? &#x27;r&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[2] = (st.st_mode &amp; S_IWUSR) ? &#x27;w&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[3] = (st.st_mode &amp; S_IXUSR) ? &#x27;x&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[4] = (st.st_mode &amp; S_IRGRP) ? &#x27;r&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[5] = (st.st_mode &amp; S_IWGRP) ? &#x27;w&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[6] = (st.st_mode &amp; S_IXGRP) ? &#x27;x&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[7] = (st.st_mode &amp; S_IROTH) ? &#x27;r&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[8] = (st.st_mode &amp; S_IWOTH) ? &#x27;w&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[9] = (st.st_mode &amp; S_IXOTH) ? &#x27;x&#x27; : &#x27;-&#x27;;</span><br><span class="line">            str[10] = &#x27;\0&#x27;;</span><br><span class="line">            tm = localtime(&amp;st.st_mtime);</span><br><span class="line">            strftime(tmbuffer, fullpath_size, &quot;%m月 %H:%M&quot;, tm);</span><br><span class="line">            pw = getpwuid(st.st_uid);</span><br><span class="line">            gr = getgrgid(st.st_gid);</span><br><span class="line">            printf(&quot;%s %lu &quot;, str, st.st_nlink);</span><br><span class="line">            if(!pw) printf(&quot;%d &quot;,st.st_uid);</span><br><span class="line">            else printf(&quot;%s &quot;,pw-&gt;pw_name);</span><br><span class="line">            if(!gr) printf(&quot;%d &quot;,st.st_gid);</span><br><span class="line">            else printf(&quot;%s &quot;,gr-&gt;gr_name);</span><br><span class="line">            printf(&quot;%ld %s %s%s%s\n&quot;, st.st_size, tmbuffer, getColor(st), dp[i]-&gt;d_name, COLOR_RESET);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    if(command &amp; CR) &#123;</span><br><span class="line">        int Rarr_size = 1024;</span><br><span class="line">        char** Rarr  = (char**)malloc(sizeof(char*) * Rarr_size);</span><br><span class="line">        int count = 0;</span><br><span class="line">        for(int i = 0; i &lt; n &amp;&amp; !(command &amp; Cr); i++) &#123;</span><br><span class="line">            if(strcmp(dp[i]-&gt;d_name, &quot;.&quot;) == 0 || strcmp(dp[i]-&gt;d_name, &quot;..&quot;) == 0) continue;</span><br><span class="line">            snprintf(fullpath, fullpath_size, &quot;%s/%s&quot;, dirpath, dp[i]-&gt;d_name);</span><br><span class="line">            lstat(fullpath, &amp;st);</span><br><span class="line">            if(S_ISDIR(st.st_mode) &amp;&amp; shouldPrintA(command, dp[i])) &#123;</span><br><span class="line">                if((int)strlen(fullpath) &gt;= (fullpath_size + 1)) &#123;</span><br><span class="line">                    fullpath_size *= 2;</span><br><span class="line">                    fullpath = (char*)realloc(fullpath, sizeof(char) * fullpath_size);</span><br><span class="line">                &#125;</span><br><span class="line">                Rarr[count] = (char*)malloc(sizeof(char) * (fullpath_size + 1));</span><br><span class="line">                strcpy(Rarr[count], fullpath);</span><br><span class="line">                count++;</span><br><span class="line">                if(count &gt;= Rarr_size) &#123;</span><br><span class="line">                    Rarr_size *= 2;</span><br><span class="line">                    Rarr = (char**)realloc(Rarr, sizeof(char*) * Rarr_size);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        for(int i = n - 1; i &gt;= 0 &amp;&amp; command &amp; Cr; i--) &#123;</span><br><span class="line">            if(strcmp(dp[i]-&gt;d_name, &quot;.&quot;) == 0 || strcmp(dp[i]-&gt;d_name, &quot;..&quot;) == 0) continue;</span><br><span class="line">            snprintf(fullpath, fullpath_size, &quot;%s/%s&quot;, dirpath, dp[i]-&gt;d_name);</span><br><span class="line">            lstat(fullpath, &amp;st);</span><br><span class="line">            if(S_ISDIR(st.st_mode) &amp;&amp; shouldPrintA(command, dp[i])) &#123;</span><br><span class="line">                if((int)strlen(fullpath) &gt;= (fullpath_size + 1)) &#123;</span><br><span class="line">                    fullpath_size *= 2;</span><br><span class="line">                    fullpath = (char*)realloc(fullpath, sizeof(char) * fullpath_size);</span><br><span class="line">                &#125;</span><br><span class="line">                Rarr[count] = (char*)malloc(sizeof(char) * fullpath_size + 1);</span><br><span class="line">                strcpy(Rarr[count], fullpath);</span><br><span class="line">                count++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        for(int i = 0; i &lt; count; i++) &#123;</span><br><span class="line">            printf(&quot;\n%s:\n&quot;, Rarr[i]);</span><br><span class="line">            listFiles(Rarr[i], command, tmpargc);</span><br><span class="line">        &#125;</span><br><span class="line">        for(int i = 0; i &lt; count; i++) free(Rarr[i]);</span><br><span class="line">        free(Rarr);</span><br><span class="line">    &#125;</span><br><span class="line">    for(int i = 0; i &lt; n; i++) free(dp[i]);</span><br><span class="line">    free(dp);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int whatCommand(int argc, char* argv[]) &#123;</span><br><span class="line">    int command = 0, enter = 1;</span><br><span class="line">    if(argc == 1) return 0;</span><br><span class="line">    for(int i = 1; i &lt; argc; ++i) &#123;</span><br><span class="line">        if(argv[i][0] != &#x27;-&#x27;) continue;</span><br><span class="line">        else &#123;</span><br><span class="line">            while(argv[i][enter] != &#x27;\0&#x27;) &#123;</span><br><span class="line">                switch (argv[i][enter++]) &#123;</span><br><span class="line">                    case &#x27;a&#x27;:command |= Ca;break;</span><br><span class="line">                    case &#x27;l&#x27;:command |= Cl;break;</span><br><span class="line">                    case &#x27;R&#x27;:command |= CR;break;</span><br><span class="line">                    case &#x27;t&#x27;:command |= Ct;break;</span><br><span class="line">                    case &#x27;r&#x27;:command |= Cr;break;</span><br><span class="line">                    case &#x27;i&#x27;:command |= Ci;break;</span><br><span class="line">                    case &#x27;s&#x27;:command |= Cs;break;</span><br><span class="line">                    default:fprintf(stderr, &quot;可用选项：-a, -l, -R, -t, -r, -i, -s\n&quot;);</span><br><span class="line">                       exit(EXIT_FAILURE);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return command;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int CompareListNormal(const struct dirent** a, const struct dirent** b) &#123;</span><br><span class="line">    return strcmp((*a)-&gt;d_name, (*b)-&gt;d_name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void printWithis(int command, struct stat* st, int n, struct dirent** dpall) &#123;</span><br><span class="line">    if(command &amp; Ci) &#123;</span><br><span class="line">        int mask = 1;</span><br><span class="line">        long int tmp = st-&gt;st_ino;</span><br><span class="line">        do &#123;</span><br><span class="line">            tmp /= 10;</span><br><span class="line">            mask++;</span><br><span class="line">        &#125;while(tmp &gt; 9);</span><br><span class="line">        int length = maxFileLength(command, dpall, n, 1);</span><br><span class="line"></span><br><span class="line">        printf(&quot;%*lu &quot;, length, st-&gt;st_ino);</span><br><span class="line">    &#125;</span><br><span class="line">    if(command &amp; Cs) &#123;</span><br><span class="line">        int mask = 1;</span><br><span class="line">        long int tmp = st-&gt;st_blocks / 2;</span><br><span class="line">        do &#123;</span><br><span class="line">            tmp /= 10;</span><br><span class="line">            mask++;</span><br><span class="line">        &#125;while(tmp &gt; 9);</span><br><span class="line">        int length = maxFileLength(command, dpall, n, 2);</span><br><span class="line"></span><br><span class="line">        printf(&quot;%*ld &quot;, length, st-&gt;st_blocks / 2);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int HowManyDirpath(int argc, char* argv[]) &#123;</span><br><span class="line">    int cnt = 0;</span><br><span class="line">    for(int i = 1; i &lt; argc; i++) if(argv[i][0] != &#x27;-&#x27;) cnt++;</span><br><span class="line">    return cnt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int shouldPrintA(int command, struct dirent* dp) &#123;</span><br><span class="line">    if(dp-&gt;d_name[0] == &#x27;.&#x27; || strcmp(dp-&gt;d_name, &quot;..&quot;) == 0) return (command &amp; Ca);</span><br><span class="line">    return 1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int maxFileLength(int command, struct dirent** dp, int n, int res) &#123;</span><br><span class="line">    int max_name = 0, max_i = 0, max_s = 0, r = 0;</span><br><span class="line">    struct stat st;</span><br><span class="line">    for(int i = 0; i &lt; n; ++i) &#123;</span><br><span class="line">        if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">        max_name = ((int)strlen(dp[i]-&gt;d_name) &gt;= max_name ) ? (int)strlen(dp[i]-&gt;d_name) : max_name;</span><br><span class="line">    &#125;</span><br><span class="line">    for(int i = 0; i &lt; n &amp;&amp; command &amp; Ci; ++i) &#123;</span><br><span class="line">        if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">        int mask = 1;</span><br><span class="line">        int tmp = dp[i]-&gt;d_ino;</span><br><span class="line">        do &#123;</span><br><span class="line">            tmp /= 10;</span><br><span class="line">            mask++;</span><br><span class="line">        &#125;while(tmp &gt; 9);</span><br><span class="line">        max_i = mask &gt;= max_i ? mask : max_i;</span><br><span class="line">    &#125;</span><br><span class="line">    for(int i = 0; i &lt; n &amp;&amp; command &amp; Cs; ++i) &#123;</span><br><span class="line">        if(!shouldPrintA(command, dp[i])) continue;</span><br><span class="line">        lstat(dp[i]-&gt;d_name, &amp;st);</span><br><span class="line">        int mask = 1;</span><br><span class="line">        int tmp = st.st_blocks;</span><br><span class="line">        do &#123;</span><br><span class="line">            tmp /= 10;</span><br><span class="line">            mask++;</span><br><span class="line">        &#125;while(tmp &gt; 9);</span><br><span class="line">        max_s = mask &gt;= max_s ? mask : max_s;</span><br><span class="line">    &#125;</span><br><span class="line">    switch (res) &#123;</span><br><span class="line">        case 0:r = max_name + max_i + max_s;break;</span><br><span class="line">        case 1:r = max_i;break;</span><br><span class="line">        case 2:r = max_s;break;</span><br><span class="line">        case 3:r = max_name;break;</span><br><span class="line">    &#125;</span><br><span class="line">    return r; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>在linux系统上使用yolov8模型</title>
    <url>/2026/03/26/%E5%9C%A8linux%E7%B3%BB%E7%BB%9F%E4%B8%8A%E4%BD%BF%E7%94%A8yolov8%E6%A8%A1%E5%9E%8B/</url>
    <content><![CDATA[<h1 id="在linux系统上使用yolov8模型"><a href="#在linux系统上使用yolov8模型" class="headerlink" title="在linux系统上使用yolov8模型"></a>在linux系统上使用yolov8模型</h1><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>大家好，本周的知识分享内容是使用yolov8来训练自己的数据集，最后将训练好的模型投入使用。</p>
<h2 id="深度学习模型"><a href="#深度学习模型" class="headerlink" title="深度学习模型"></a>深度学习模型</h2><p>深度学习模型是机器学习的一个子集，其核心是构建由多个“层”组成的人工神经网络，通过层次化特征学习，从大规模数据中自动提取从低级到高级的抽象表示。</p>
<p>分类:</p>
<ul>
<li><p>计算机视觉:yolo,自研模型</p>
</li>
<li><p>自然语言处理:deepseek,GPT,gemini,transformer,自研模型</p>
</li>
<li><p>多模态处理:GPT,自研模型</p>
</li>
</ul>
<h2 id="计算机视觉"><a href="#计算机视觉" class="headerlink" title="计算机视觉"></a>计算机视觉</h2><p>计算机视觉是人工智能的一个核心研究领域，旨在赋予计算机“看”和理解图像或视频内容的能力，其目标是从视觉数据中自动提取、分析、理解有用信息，并做出决策或表达。</p>
<h2 id="yolo介绍"><a href="#yolo介绍" class="headerlink" title="yolo介绍"></a>yolo介绍</h2><ul>
<li><p><a href="https://docs.ultralytics.com/zh/">yolo官方文档</a></p>
</li>
<li><p><a href="https://docs.ultralytics.com/zh/#yolo-a-brief-history">历史版本</a></p>
</li>
<li><p><a href="https://docs.ultralytics.com/zh/tasks/">应用场景</a></p>
</li>
</ul>
<p><code>YOLO</code>（You Only Look Once）是一种流行的物体检测和图像分割模型。<code>YOLO</code> 于 2015 年推出，因其高速和高精度而广受欢迎。</p>
<ul>
<li><p><code>目标检测</code>是一项涉及识别图像或视频流中目标的位置和类别的任务。</p>
<ul>
<li>目标检测器的输出是一组边界框，这些边界框包围了图像中的目标，以及每个框的类别标签和置信度分数。当您需要在场景中识别感兴趣的目标，但不需要确切知道目标在哪里或其确切形状时，目标检测是一个不错的选择。</li>
</ul>
</li>
<li><p><code>实例分割</code>比对象检测更进一步，包括识别图像中的各个对象并将它们与图像的其余部分分割开来。</p>
<ul>
<li>实例分割模型的输出是一组掩码或轮廓，它们勾勒出图像中每个对象，以及每个对象的类别标签和置信度分数。 当您不仅需要知道对象在图像中的位置，还需要知道它们的精确形状时，实例分割非常有用。</li>
</ul>
</li>
<li><p><code>图像分类</code>是三个任务中最简单的，它涉及将整个图像分类到一组预定义的类别中。</p>
<ul>
<li>图像分类器的输出是单个类别标签和一个置信度分数。当您只需要知道图像属于哪个类别，而不需要知道该类别的对象位于何处或其确切形状时，图像分类非常有用。</li>
</ul>
</li>
</ul>
<h3 id="前置知识"><a href="#前置知识" class="headerlink" title="前置知识"></a>前置知识</h3><p>虚拟环境:是将项目需要用到的依赖放到一个统一的环境内，防止不同项目之间，需要的依赖不同而发生冲突，很有助于项目管理，yolo的官方文档中提到了很多虚拟环境工具，如conda，pythonVenv等，此处我使用的是conda，他的操作更简单，并且自动安装了CUDA。</p>
<p>yaml文件:在yolov8训练过程中需要配置的文件。</p>
<p>置信度:是目标检测模型中，表示预测结果可信程度的一个概率值，范围在 [0, 1] 之间。数值越高，表示模型对自己预测的结果越确信。</p>
<p>数据集:是机器学习和人工智能领域中，用于训练、验证和测试模型的结构化数据集合。它是深度学习模型的“教材”——模型通过学习数据集中的样本与标签之间的映射关系，来获得解决特定任务的能力。</p>
<p>训练集:<br>训练集是用于训练模型的数据集合，模型通过学习训练集中的图像和对应的标注信息，来调整自身的参数（权重和偏置），从而掌握识别目标的能力。</p>
<p>验证集:<br>验证集是用于评估模型训练效果和调优超参数的数据集合，模型在训练过程中定期在验证集上进行测试，但不参与参数更新。</p>
<p>测试集:<br>测试集是用于最终评估模型泛化能力的数据集合，只在模型训练完成后使用一次，用于报告模型的真实性能。</p>
<p>标签:<br>class_id	类别编号(从0开始)<br>x_center	框中心点x坐标(归一化)<br>y_center	框中心点y坐标(归一化)<br>width	    框宽度(归一化)<br>height	    框高度(归一化)  </p>
<p>过拟合:是指模型在训练集上表现非常好（损失很低、准确率很高），但在验证集或测试集上表现很差的现象。简单来说：模型死记硬背了训练数据，但没有学到通用规律，导致对新数据泛化能力差。</p>
<p>张量即多维数组。在计算机视觉相关的模型中，一张图片可以被转换为张量，作为参数被模型使用。</p>
<p>张量的存储可以在cpu上，gpu上，对于gpu上的张量可以使用CUDA进行加速。</p>
<p>在yolo模型中，张量作为参数，yolo模型作为函数，接受参数对其进行训练。</p>
<p>损失:是一个标量张量，表示模型预测结果与真实标签之间的差距。损失越小，模型预测越准。</p>
<p>损失函数:用来检测损失的程度。</p>
<p>梯度:是一个张量，表示了每个参数应该往哪个方向调整，才能让损失变小。  </p>
<p>前向传播：输入数据逐层计算得到预测结果和损失值的过程。</p>
<p>反向传播：从损失函数出发，利用链式法则反向计算每一层参数梯度的过程。</p>
<p>梯度下降：参数沿着梯度的反方向进行迭代更新以最小化损失函数的优化算法。</p>
<p>优化器：基于梯度下降原理，决定参数更新策略的算法实现。</p>
<h3 id="必要的工具"><a href="#必要的工具" class="headerlink" title="必要的工具"></a>必要的工具</h3><ul>
<li><p>vscode:支持加入很多插件，自由度更高，更灵活。</p>
</li>
<li><p>pip:是Python的官方包管理工具。</p>
</li>
<li><p>pytorch:深度学习框架，是yolov8的核心依赖。</p>
</li>
<li><p>conda:一个开源的包管理系统和环境管理系统。能创建隔离的虚拟环境，避免不同项目之间的依赖冲突。</p>
</li>
<li><p>CUDA:调用N卡的GPU对模型进行训练。</p>
</li>
</ul>
<h3 id="我自己的电脑配置"><a href="#我自己的电脑配置" class="headerlink" title="我自己的电脑配置"></a>我自己的电脑配置</h3><ul>
<li><p>cpu:AMD Ryzen 7 5800H with Radeon Graphics 8核 16线程 </p>
</li>
<li><p>显卡:NVIDIA GeForce RTX 3060</p>
</li>
<li><p>内存:16GB</p>
</li>
<li><p>OS:ubuntu 25.04</p>
</li>
</ul>
<h2 id="环境安装"><a href="#环境安装" class="headerlink" title="环境安装"></a>环境安装</h2><h3 id="虚拟环境配置"><a href="#虚拟环境配置" class="headerlink" title="虚拟环境配置"></a>虚拟环境配置</h3><ol>
<li>安装conda</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/Miniconda3-latest-Linux-x86_64.sh -O ~/miniconda.sh</span><br><span class="line"></span><br><span class="line">bash ~/miniconda.sh -b -p $HOME/miniconda3</span><br><span class="line"></span><br><span class="line">~/miniconda3/bin/conda init fish</span><br><span class="line"></span><br><span class="line">exec fish</span><br><span class="line"></span><br><span class="line">conda --version</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>配置pip</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt install pip</span><br><span class="line"></span><br><span class="line">pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple </span><br></pre></td></tr></table></figure>

<ol start="3">
<li>创建并激活虚拟环境</li>
</ol>
<p>第一次创建环境会比较慢，这里在conda的配置文件中加入清华源，移除较慢的源，再进行创建。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 配置国内源</span><br><span class="line">conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main/</span><br><span class="line">conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/free/</span><br><span class="line">conda config --add channels https://mirrors.tuna.tsinghua.edu.cn/anaconda/cloud/conda-forge/</span><br><span class="line">conda config --set show_channel_urls yes</span><br><span class="line"></span><br><span class="line"># 移除速度较慢的源</span><br><span class="line">conda config --remove channels defaults</span><br><span class="line"></span><br><span class="line"># 接受 Anaconda 官方仓库（repo.anaconda.com）的服务条款</span><br><span class="line">conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main</span><br><span class="line">conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r</span><br><span class="line"></span><br><span class="line"># 创建虚拟环境</span><br><span class="line">conda create -n yolo python=3.10 -y</span><br><span class="line"></span><br><span class="line"># 激活虚拟环境</span><br><span class="line">conda activate yolo</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>安装pytorch</li>
</ol>
<ul>
<li><a href="https://pytorch.org/">pytorch官方文档</a></li>
</ul>
<p>由于在训练模型时需要用到CUDA，所以需要查询自己n卡的cuda的最高支持版本，使用下面命令查询cuda版本<code>nvidia-smi</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pip install torch==1.13.1+cu116 torchvision==0.14.1+cu116 torchaudio==0.13.1 --extra-index-url https://download.pytorch.org/whl/cu116</span><br></pre></td></tr></table></figure>

<ol start="5">
<li>下载ultralytics(yolov8)</li>
</ol>
<p>这里直接克隆官方的源码，并且使用pip，以可修改源码的形式安装。</p>
<p>使用可修改源码的形式安装，有助于我们定制个人化的yolov8模型，更有助于对yolov8源码的学习。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">git clone git@github.com:ultralytics/ultralytics.git</span><br><span class="line"></span><br><span class="line">cd ultralytics</span><br><span class="line"></span><br><span class="line">pip install -e .</span><br></pre></td></tr></table></figure>

<h2 id="模型预测"><a href="#模型预测" class="headerlink" title="模型预测"></a>模型预测</h2><p>在ultralytics目录下，直接运行以下命令。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">yolo predict model=yolov8n.pt source=&#x27;ultralytics/assets/bus.jpg&#x27;</span><br></pre></td></tr></table></figure>

<h2 id="数据处理"><a href="#数据处理" class="headerlink" title="数据处理"></a>数据处理</h2><h3 id="数据的选择"><a href="#数据的选择" class="headerlink" title="数据的选择"></a>数据的选择</h3><ol>
<li>数据要有真实应用场景，最好不要摆拍。</li>
<li>选择的图片要尽可能复杂多样，有白天拍摄也有夜晚拍摄，覆盖多种情况。</li>
<li>样本尽可能多些。</li>
<li>要和目标检测的对象相关。</li>
</ol>
<h3 id="数据集获取与标注"><a href="#数据集获取与标注" class="headerlink" title="数据集获取与标注"></a>数据集获取与标注</h3><p>数据集的获取有如下代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import cv2</span><br><span class="line"></span><br><span class="line">video = cv2.VideoCapture(&quot;./puerzi.mp4&quot;)</span><br><span class="line">num = 0</span><br><span class="line">save_step = 30</span><br><span class="line"></span><br><span class="line">while True:</span><br><span class="line">    ret, frame = video.read()</span><br><span class="line">    if not ret:</span><br><span class="line">        break</span><br><span class="line">    num += 1</span><br><span class="line">    if num % save_step == 0:</span><br><span class="line">        cv2.imwrite(&quot;./demo_images/&quot; + str(num) + &quot;.jpg&quot;, frame)</span><br></pre></td></tr></table></figure>

<p>对于数据集的标注，市面上有很多种工具和网站，<code>www.makesense.ai</code>是一个在线的标注工具，提高了标注效率。并且支持大批量文件导入，压缩包文件导出。</p>
<h2 id="数据训练"><a href="#数据训练" class="headerlink" title="数据训练"></a>数据训练</h2><h3 id="文件结构方面"><a href="#文件结构方面" class="headerlink" title="文件结构方面"></a>文件结构方面</h3><p>结构如图：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ultralytics/                # ultralytics 根目录</span><br><span class="line">├── datasets/               # 数据集根目录</span><br><span class="line">│   └── your_dataset/       # 你的数据集名称（如：coco_custom、helmet_detection）</span><br><span class="line">│       ├── images/         # 图片文件夹</span><br><span class="line">│       │   ├── train/      # 训练集图片</span><br><span class="line">│       │   └── val/        # 验证集图片</span><br><span class="line">│       └── labels/         # 标签文件夹（txt格式）</span><br><span class="line">│           ├── train/      # 训练集标签</span><br><span class="line">│           └── val/        # 验证集标签</span><br><span class="line">├── ultralytics/            # 源码目录</span><br><span class="line">└── ...</span><br></pre></td></tr></table></figure>

<h3 id="代码方面"><a href="#代码方面" class="headerlink" title="代码方面"></a>代码方面</h3><p>在ultralytics的根目录下创建yaml文件和py文件，yaml文件用来确定待训练数据的位置，和训练对象。py文件用来执行YOLOV8的训练。</p>
<p>在py文件中，可以自行配置各种训练参数，如：</p>
<ul>
<li><p>workers: 是用于并行加载数据的进程数量。当训练模型时，CPU需要不断读取图像、解码、进行数据增强等预处理操作，然后将处理好的数据传递给GPU进行训练。在win系统下，此处需要设置为0,linux系统下设置为1～8(默认为8)即可。</p>
</li>
<li><p>epochs: 训练轮数。</p>
</li>
</ul>
<p>epoch 1-50:   快速下降阶段，模型快速学习基本特征<br>epoch 50-150: 缓慢下降阶段，模型优化细节<br>epoch 150-300: 收敛阶段，loss基本不再下降<br>epoch &gt;300:   过拟合风险增加</p>
<ul>
<li>batch: 是模型每次迭代训练时处理的图像数量。例如batch&#x3D;16意味着每次同时处理16张图像，计算梯度，然后更新一次模型参数。</li>
</ul>
<h3 id="查看训练结果"><a href="#查看训练结果" class="headerlink" title="查看训练结果"></a>查看训练结果</h3><p>在runs&#x2F;detect目录下，存放着YOLOV8对图片的训练结果以及训练的可视化过程。</p>
<p>在weights目录下，存放着best.pt和last.pt文件，一个是训练出来最好的模型，另一个供下一次训练时继续使用。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">yolo detect predict model=runs/detect/()/weights/best.pt  source=() show=True</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>本次知识分享，主要内容包括认识了深度学习模型的种类，计算机视觉概要，初识yolo。学习了如何使用yolov8模型，从环境安装，模型部署，模型预测，数据集收集与标注，模型训练的一系列过程。</p>
]]></content>
  </entry>
  <entry>
    <title>慢慢走下去</title>
    <url>/2026/03/23/%E6%85%A2%E6%85%A2%E8%B5%B0%E4%B8%8B%E5%8E%BB/</url>
    <content><![CDATA[<p>我自幼喜欢计算机,却没什么大理想,只想在快节奏的时代里留下自己的足迹。</p>
<p>本网站希望可以带给你快乐。ヽ(●´∀&#96;●)ﾉ</p>
<p align="center" style="font-size: 24px; font-weight: bold;">行香子·在一中楼</p>
<p align="center">
梅柳葵菊，花谢丰收。<br>
呢喃却，了了金秋。你追我赶，慢慢悠悠。<br>
走菜花园，操场道，教学楼。<br>
山高多险，风急难驻。<br>
忘离别，晓向天涯。你说他逗，将也临冬，<br>
过高一闹，高三静，总轻柔。<br>
<br>]]></content>
  </entry>
  <entry>
    <title>不要轻易修改你的linux系统，除非你一无所知</title>
    <url>/2025/12/22/%E4%B8%8D%E8%A6%81%E8%BD%BB%E6%98%93%E4%BF%AE%E6%94%B9%E4%BD%A0%E7%9A%84linux%E7%B3%BB%E7%BB%9F%EF%BC%8C%E9%99%A4%E9%9D%9E%E4%BD%A0%E4%B8%80%E6%97%A0%E6%89%80%E7%9F%A5/</url>
    <content><![CDATA[<h2 id="不要轻易修改你的linux系统，除非你一无所知"><a href="#不要轻易修改你的linux系统，除非你一无所知" class="headerlink" title="不要轻易修改你的linux系统，除非你一无所知"></a>不要轻易修改你的linux系统，除非你一无所知</h2><p>为什么突然想写这样一篇文章?  </p>
<ul>
<li><p>起因：<br>起初在小组，看到学长的hyprland如此生动有趣，值得把玩，那为什么我也不试一试呢？说时迟那时快，我直接打开chrome，搜索ubuntu安装hyprland。</p>
</li>
<li><p>发展：<br>果然，看到网上的预览效果十分震撼我弱小的心灵，对于一个linux新用户来说，一个可自定义的桌面十分吸引人。也没管是怎么个说法，就把这份脚本下载下来了。下载的时间十分漫长，足够一个193身高的人安安静静地吃完一份晚饭，顺便买瓶阿萨姆。当我回到了小组工位上，看到hyprland下载完成。我直接注销用户，切换桌面为hyprland。他终于来了。</p>
</li>
<li><p>高潮：<br>hyprland凭借着他的高自由度，和充满geek风格的桌面深深吸引了我，从waybar到终端，但还有一点小小的不足，我的双显示屏并不能正常使用。那好吧，经过一番设置，也还算成功，但，无论怎样，总会有一点点小瑕疵，让这个双显示屏的切换不够丝滑。之后询问了一下d老师，d老师说这是这样那样的问题，我信了，结果，整个桌面开始变得不再正常，从waybar到终端。甚至是24级的学长都无能为力，难道只能放弃了吗？犹豫再三，我选择重装系统，就是这么暴力。</p>
</li>
<li><p>结尾：<br>看似故事已经结束，但故事才刚刚开始。在之后的这几天内，我似乎陷入了while 1 循环，找不到循环结束条件。从安装linux mint 的Xfce版本，到安装ubuntu25.04 又升级到25.10，没有一次是称心如意的。经过我来来回回的装系统，删系统，甚至我的小u盘也败下阵来，一插入就开始显示不正常的消息。结果这场闹剧以安装ubuntu25.04收尾。</p>
</li>
<li><p>结语：<br>折腾是一件好事吗？这浪费了时间，没有真正意义上提升了代码编写能力和思考业务的能力，而且徒增烦恼。但也并非如此。还记得刷视频的时候，有位文学先生，对读书的意义作出了一份恰当的总结：”读书的意义并不在于记住什么知识，而是像喝水一样，补充养分，涵养精神。“计算机的学习也大概如此。</p>
</li>
</ul>
]]></content>
      <tags>
        <tag>日常倒霉趣事</tag>
      </tags>
  </entry>
  <entry>
    <title>西邮 Linux 兴趣小组 2023 纳新面试题</title>
    <url>/2025/10/27/%E8%A5%BF%E9%82%AE%20Linux%20%E5%85%B4%E8%B6%A3%E5%B0%8F%E7%BB%84%202023%20%E7%BA%B3%E6%96%B0%E9%9D%A2%E8%AF%95%E9%A2%98/</url>
    <content><![CDATA[<h1 id="西邮-Linux-兴趣小组-2023-纳新面试题"><a href="#西邮-Linux-兴趣小组-2023-纳新面试题" class="headerlink" title="西邮 Linux 兴趣小组 2023 纳新面试题"></a>西邮 Linux 兴趣小组 2023 纳新面试题</h1><blockquote>
<p>学长寄语：长期以来，西邮Linux兴趣小组的面试题以难度之高名扬西邮校内。我们作为出题人也清楚的知道这份试题略有难度。请你动手敲一下代码。别担心，若有同学能完成一半的题目，就已经十分优秀。其次，相比于题目的答案，我们对你的思路和过程更感兴趣，或许你的答案略有瑕疵，但你正确的思路和对知识的理解足以为你赢得绝大多数的分数。最后，做题的过程也是学习和成长的过程，相信本试题对你更加熟悉的掌握C语言的一定有所帮助。祝你好运。我们东区逸夫楼FZ103见</p>
</blockquote>
<ul>
<li>本题目只作为西邮 Linux 兴趣小组 2023 纳新面试的有限参考。</li>
<li>为节省版面，本试题的程序源码省去了 <code>#include</code> 指令。</li>
<li>本试题中的程序源码仅用于考察 C 语言基础，不应当作为 C 语言「代码风格」的范例。</li>
<li>所有题目编译并运行于 <em>x86_64 GNU&#x2F;Linux</em> 环境。</li>
</ul>
<h2 id="0-鼠鼠我啊，要被祸害了"><a href="#0-鼠鼠我啊，要被祸害了" class="headerlink" title="0. 鼠鼠我啊，要被祸害了"></a>0. 鼠鼠我啊，要被祸害了</h2><p>有1000瓶水，其中有一瓶有毒，小白鼠只要尝一点带毒的水，24小时后就会准时死亡。至少要多少只小白鼠才能在24小时内鉴别出哪瓶水有毒？  </p>
<p><strong>题解：</strong></p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    int b;</span><br><span class="line">    scanf(&quot;%d&quot;,&amp;b);//毒药编号</span><br><span class="line">    int number=0;</span><br><span class="line">    while(b&gt;0)</span><br><span class="line">    &#123;</span><br><span class="line">        if(b&amp;1)</span><br><span class="line">        &#123;</span><br><span class="line">            number++;</span><br><span class="line">        &#125;</span><br><span class="line">        b&gt;&gt;=1;</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;%d&quot;,number);//小鼠数量</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ul>
<li>解释：</li>
</ul>
<ol>
<li>由于有1000瓶药水，而$2^{10}$&gt;1000，故至少需要10只小鼠。</li>
</ol>
<ul>
<li>考点：<br> 通过数学工具来解决实际问题的能力</li>
</ul>
<h2 id="1-先预测一下"><a href="#1-先预测一下" class="headerlink" title="1. 先预测一下~"></a>1. 先预测一下~</h2><p>按照函数要求输入自己的姓名试试~  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">char *welcome() &#123;</span><br><span class="line">    // 请你返回自己的姓名</span><br><span class="line">&#125;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    char *a = welcome();</span><br><span class="line">    printf(&quot;Hi, 我相信 %s 可以面试成功!\n&quot;, a);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line"></span><br><span class="line">char *welcome() &#123;</span><br><span class="line">    return &quot;FRiver&quot;;</span><br><span class="line">&#125;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    char *a = welcome();</span><br><span class="line">    printf(&quot;Hi, 我相信 %s 可以面试成功!\n&quot;, a);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>输出结果：<br> <code>Hi, 我相信 FRiver 可以面试成功!</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>在welcome()函数内，返回为一个字符串，这个字符串地址被保存在*a内。</li>
<li>之后在printf函数内打印这个字符串。</li>
</ol>
<ul>
<li>考点：<br> 字符串</li>
</ul>
<h2 id="2-欢迎来到Linux兴趣小组"><a href="#2-欢迎来到Linux兴趣小组" class="headerlink" title="2. 欢迎来到Linux兴趣小组"></a>2. 欢迎来到Linux兴趣小组</h2><p>有趣的输出，为什么会这样子呢~<br>你了解 <code>sizeof()</code> 和 <code>strlen()</code> 吗？他们的区别是什么？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(void) &#123;</span><br><span class="line">    char *ptr0 = &quot;Welcome to Xiyou Linux!&quot;;</span><br><span class="line">    char ptr1[] = &quot;Welcome to Xiyou Linux!&quot;;</span><br><span class="line">    if (*ptr0 == *ptr1) &#123;</span><br><span class="line">      printf(&quot;%d\n&quot;, printf(&quot;Hello, Linux Group - 2%d&quot;, printf(&quot;&quot;)));</span><br><span class="line">    &#125;</span><br><span class="line">    int diff = ptr0 - ptr1;</span><br><span class="line">    printf(&quot;Pointer Difference: %d\n&quot;, diff);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>Hello, Linux Group - 2023</code><br> <code>Pointer Difference: 473149860(随着每次编译而变化)</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li><code>printf</code>函数内嵌套了一些<code>printf</code>函数，每个函数的返回值为其需要打印内容的字符长度。由于<code>*ptr0</code>和<code>*ptr1</code>均为<code>‘W’</code>，故相等。在<code>diff</code>变量下，保存了p<code>tr0</code>和<code>ptr1</code>的差值，由于<code>ptr1</code>是一个数组，他保存在栈上，<code>ptr2</code>指向的是一个字符串字面量，具有静态存储期。故差值不定。</li>
<li>sizeof()运算符计算对象所占内存的大小或一个数据结构的大小。strlen()函数计算字符串的字符长度。</li>
</ol>
<ul>
<li>考点：<br> printf函数返回值、存储期、sizeof()、strlen()</li>
</ul>
<h2 id="3-一切都翻倍了吗"><a href="#3-一切都翻倍了吗" class="headerlink" title="3. 一切都翻倍了吗"></a>3. 一切都翻倍了吗</h2><ol>
<li>请尝试解释一下程序的输出。  </li>
<li>请谈谈对<code>sizeof()</code>和<code>strlen()</code>的理解吧。  </li>
<li>什么是<code>sprintf()</code>，它的参数以及返回值又是什么呢？</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(void) &#123;</span><br><span class="line">    char arr[] = &#123;&#x27;L&#x27;, &#x27;i&#x27;, &#x27;n&#x27;, &#x27;u&#x27;, &#x27;x&#x27;, &#x27;\0&#x27;, &#x27;!&#x27;&#125;, str[20];</span><br><span class="line">    short num = 520;</span><br><span class="line">    int num2 = 1314;</span><br><span class="line">    printf(&quot;%zu\t%zu\t%zu\n&quot;, sizeof(*&amp;arr), sizeof(arr + 0),</span><br><span class="line">           sizeof(num = num2 + 4));</span><br><span class="line">    printf(&quot;%d\n&quot;, sprintf(str, &quot;0x%x&quot;, num) == num);</span><br><span class="line">    printf(&quot;%zu\t%zu\n&quot;, strlen(&amp;str[0] + 1), strlen(arr + 0));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>7       8       2</code><br> <code>0</code><br> <code>4       5</code></p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li><code>sizeof()</code>运算符对<code>arr</code>数组的元素数量进行计算，有<code>7</code>个元素故为<code>7</code>。<code>arr+0</code>说明了是对<code>arr</code>这个数组的入口地址计算，则其结果为<code>8</code>。由于赋值表达式的类型是左值的类型，则其大小为<code>2</code>。</li>
<li><code>sprintf</code>其类似与<code>printf</code>，唯一区别就是他会把参数放到一个<code>str</code>数组里面，返回值为传入参数的字符数。而由于该参数是以<code>16</code>进制的方式输入的且有前缀，则其返回的字符数一定不等于原字符数，整体返回<code>0</code>。</li>
<li><code>&amp;str[0]+1</code>的结果为<code>x</code>的地址，从该地址开始，计算到<code>\0</code>之前的字符数。<code>arr+0</code>的结果为<code>0</code>的地址，从该地址开始，计算到<code>\0</code>之前的字符数。</li>
</ol>
<ul>
<li>考点：<br> sizeof()、sprintf()</li>
</ul>
<h2 id="4-奇怪的输出"><a href="#4-奇怪的输出" class="headerlink" title="4. 奇怪的输出"></a>4. 奇怪的输出</h2><p>程序的输出结果是什么？解释一下为什么出现该结果吧~</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(void) &#123;</span><br><span class="line">    char a = 64 &amp; 127;</span><br><span class="line">    char b = 64 ^ 127;</span><br><span class="line">    char c = -64 &gt;&gt; 6;</span><br><span class="line">    char ch = a + b - c;</span><br><span class="line">    printf(&quot;a = %d b = %d c = %d\n&quot;, a, b, c);</span><br><span class="line">    printf(&quot;ch = %d\n&quot;, ch);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>a = 64 b = 63 c = -1</code><br> <code>ch = -128</code></p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li><code>01000000 &amp; 01111111 = 01000000</code></li>
<li><code>01000000 ^ 01111111 = 01111111</code></li>
<li><code>01000000</code>向右移6位：<code>00000001</code>，由于为负数，补码的结果为：<code>11111111</code></li>
<li>ch的结果为<code>64+63-(-1)=128</code>，这个数超过了有符号整数的范围，补码结果为<code>-128</code></li>
</ol>
<ul>
<li>考点：<br> 位运算、补码</li>
</ul>
<h2 id="5-乍一看就不想看的函数"><a href="#5-乍一看就不想看的函数" class="headerlink" title="5. 乍一看就不想看的函数"></a>5. 乍一看就不想看的函数</h2><p><em>“人们常说互联网凛冬已至，要提高自己的竞争力，可我怎么卷都卷不过别人，只好用一些奇技淫巧让我的代码变得高深莫测。”</em><br>这个<code>func()</code>函数的功能是什么？是如何实现的？  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int func(int a, int b) &#123;</span><br><span class="line">    if (!a) return b;</span><br><span class="line">    return func((a &amp; b) &lt;&lt; 1, a ^ b);</span><br><span class="line">&#125;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    int a = 4, b = 9, c = -7;</span><br><span class="line">    printf(&quot;%d\n&quot;, func(a, func(b, c)));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>6</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>main函数内打印的数字，为整个函数传参后的返回值，参数内嵌套了函数，函数本身又在递归。</li>
<li>在函数内，先判断a的值，a的值为0时，返回b的值，否则递归函数。</li>
</ol>
<ul>
<li>考点：<br> 递归</li>
</ul>
<h2 id="6-自定义过滤"><a href="#6-自定义过滤" class="headerlink" title="6. 自定义过滤"></a>6. 自定义过滤</h2><p>请实现<code>filter()</code>函数：过滤满足条件的数组元素。<br>提示：使用函数指针作为函数参数并且你需要为新数组分配空间。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">typedef int (*Predicate)(int);</span><br><span class="line">int *filter(int *array, int length, Predicate predicate,</span><br><span class="line">            int *resultLength); /*补全函数*/</span><br><span class="line">int isPositive(int num) &#123; return num &gt; 0; &#125;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    int array[] = &#123;-3, -2, -1, 0, 1, 2, 3, 4, 5, 6&#125;;</span><br><span class="line">    int length = sizeof(array) / sizeof(array[0]);</span><br><span class="line">    int resultLength;</span><br><span class="line">    int *filteredNumbers = filter(array, length, isPositive,</span><br><span class="line">                                  &amp;resultLength);</span><br><span class="line">    for (int i = 0; i &lt; resultLength; i++) &#123;</span><br><span class="line">      printf(&quot;%d &quot;, filteredNumbers[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;\n&quot;);</span><br><span class="line">    free(filteredNumbers);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong>  </p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;string.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line">#include&lt;assert.h&gt;</span><br><span class="line"></span><br><span class="line">typedef int (*Predicate)(int);</span><br><span class="line"></span><br><span class="line">int *filter(int *array, int length, Predicate predicate,</span><br><span class="line">            int *resultLength); /*补全函数*/</span><br><span class="line"></span><br><span class="line">int isPositive(int num) &#123; return num &gt; 0; &#125;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    int array[] = &#123;-3, -2, -1, 0, 1, 2, 3, 4, 5, 6&#125;;</span><br><span class="line">    int length = sizeof(array) / sizeof(array[0]);</span><br><span class="line">    int resultLength;</span><br><span class="line">    int *filteredNumbers = filter(array, length, isPositive, &amp;resultLength);</span><br><span class="line">    for (int i = 0; i &lt; resultLength; i++) &#123;</span><br><span class="line">      printf(&quot;%d &quot;, filteredNumbers[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;\n&quot;);</span><br><span class="line">    free(filteredNumbers);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int *filter(int *array, int length, Predicate predicate,int *resultLength)</span><br><span class="line">&#123;</span><br><span class="line">	int cnt=0;</span><br><span class="line">	int* ar=(int*)malloc(sizeof(int)*length);</span><br><span class="line">	assert(ar);</span><br><span class="line">	int i;</span><br><span class="line"></span><br><span class="line">	for(i=0;i&lt;length;i++)</span><br><span class="line">	&#123;</span><br><span class="line">		if((*predicate)(array[i]))</span><br><span class="line">		&#123;</span><br><span class="line">			ar[cnt]=array[i];</span><br><span class="line">			cnt++;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	*resultLength=cnt;</span><br><span class="line">	return ar;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>输出结果：<br><code>1 2 3 4 5 6 </code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>首先定义了一个函数指针，指向的函数传入int类型的参数，返回int类型的值。</li>
<li>在过滤函数内，首先动态分配内存，之后使用判断函数进行判断是否大于0,之后进行赋值，并且记录有效值的个数，最后返回刚刚动态分配内存的地址，进入主函数。</li>
</ol>
<ul>
<li>考点：<br> 函数指针、动态分配内存</li>
</ul>
<h2 id="7-静…态…"><a href="#7-静…态…" class="headerlink" title="7. 静…态…"></a>7. 静…态…</h2><ol>
<li>如何理解关键字<code>static</code>？  </li>
<li><code>static</code>与变量结合后有什么作用？  </li>
<li><code>static</code>与函数结合后有什么作用？  </li>
<li><code>static</code>与指针结合后有什么作用？  </li>
<li><code>static</code>如何影响内存分配？</li>
</ol>
<p><strong>题解：</strong></p>
<ul>
<li>解释：</li>
</ul>
<ol>
<li>即“静态”。</li>
<li>将变量、函数、指针声明为，一个具有文件作用域，内部连接，静态存储期的变量。</li>
<li>static的对象被放在静态存储区。</li>
</ol>
<ul>
<li>考点：<br> static</li>
</ul>
<h2 id="8-救命！指针！"><a href="#8-救命！指针！" class="headerlink" title="8. 救命！指针！"></a>8. 救命！指针！</h2><p>数组指针是什么？指针数组是什么？函数指针呢？用自己的话说出来更好哦，下面数据类型的含义都是什么呢？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int (*p)[10];</span><br><span class="line">const int* p[10];</span><br><span class="line">int (*f1(int))(int*, int);</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li>解释：</li>
</ul>
<ol>
<li><code>(*p)[10]</code>是一个指向含有10个int类型元素数组的指针。</li>
<li><code>* p[10]</code>是一个含有10个指向int类型指针元素的数组，且他被const修饰，无法修改其数组内容。</li>
<li><code>f1</code>是一个接受int类型参数的函数，返回值是一个指针，即指向一个参数为int*和int类型的函数，指向的函数返回类型为int。即f1是一个函数指针。</li>
</ol>
<ul>
<li>考点：<br> 指针数组、数组指针、函数指针</li>
</ul>
<h2 id="9-咋不循环了"><a href="#9-咋不循环了" class="headerlink" title="9. 咋不循环了"></a>9. 咋不循环了</h2><p>程序直接运行，输出的内容是什么意思？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(int argc, char* argv[]) &#123;</span><br><span class="line">    printf(&quot;[%d]\n&quot;, argc);</span><br><span class="line">    while (argc) &#123;</span><br><span class="line">      ++argc;</span><br><span class="line">    &#125;</span><br><span class="line">    int i = -1, j = argc, k = 1;</span><br><span class="line">    i++ &amp;&amp; j++ || k++;</span><br><span class="line">    printf(&quot;i = %d, j = %d, k = %d\n&quot;, i, j, k);</span><br><span class="line">    return EXIT_SUCCESS;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>[1]</code><br> <code>i = 0, j = 1, k = 2</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>argc一开始为1，之后进入while循环，argc一直递增(有补码)直到argc为0，退出循环。</li>
<li>表达式内，i&#x3D;-1为真，j&#x3D;0为假，k&#x3D;1为真，没有短路，且三个变量均递增。</li>
</ol>
<ul>
<li>考点：<br> 短路、argc</li>
</ul>
<h2 id="10-到底是不是TWO"><a href="#10-到底是不是TWO" class="headerlink" title="10. 到底是不是TWO"></a>10. 到底是不是TWO</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define CAL(a) a * a * a</span><br><span class="line">#define MAGIC_CAL(a, b) CAL(a) + CAL(b)</span><br><span class="line">int main(void) &#123;</span><br><span class="line">  int nums = 1;</span><br><span class="line">  if(16 / CAL(2) == 2) &#123;</span><br><span class="line">    printf(&quot;I&#x27;m TWO(ﾉ&gt;ω&lt;)ﾉ\n&quot;);</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    int nums = MAGIC_CAL(++nums, 2);</span><br><span class="line">  &#125;</span><br><span class="line">  printf(&quot;%d\n&quot;, nums);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>1</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>CAL宏定义，没有加()，会直接“贴”到代码内，使得<code>16 / CAL(2)</code>结果为<code>32</code></li>
<li>进入else语句内，创建了一个新的块作用域的nums，之后又被销毁，故main内的nums没有被改动，仍为<code>1</code>。</li>
</ol>
<ul>
<li>考点：<br> 宏、自动变量</li>
</ul>
<h2 id="11-克隆困境"><a href="#11-克隆困境" class="headerlink" title="11. 克隆困境"></a>11. 克隆困境</h2><p>试着运行一下程序，为什么会出现这样的结果？<br>直接将<code>s2</code>赋值给<code>s1</code>会出现哪些问题，应该如何解决？请写出相应代码。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct Student &#123;</span><br><span class="line">    char *name;</span><br><span class="line">    int age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">void initializeStudent(struct Student *student, const char *name,</span><br><span class="line">                       int age) &#123;</span><br><span class="line">    student-&gt;name = (char *)malloc(strlen(name) + 1);</span><br><span class="line">    strcpy(student-&gt;name, name);</span><br><span class="line">    student-&gt;age = age;</span><br><span class="line">&#125;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    struct Student s1, s2;</span><br><span class="line">    initializeStudent(&amp;s1, &quot;Tom&quot;, 18);</span><br><span class="line">    initializeStudent(&amp;s2, &quot;Jerry&quot;, 28);</span><br><span class="line">    s1 = s2;</span><br><span class="line">    printf(&quot;s1的姓名: %s 年龄: %d\n&quot;, s1.name, s1.age);</span><br><span class="line">    printf(&quot;s2的姓名: %s 年龄: %d\n&quot;, s2.name, s2.age);</span><br><span class="line">    free(s1.name);</span><br><span class="line">    free(s2.name);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct Student &#123;</span><br><span class="line">    char *name;</span><br><span class="line">    int age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">void initializeStudent(struct Student *student, const char *name,</span><br><span class="line">                       int age) &#123;</span><br><span class="line">    student-&gt;name = (char *)malloc(strlen(name) + 1);</span><br><span class="line">    strcpy(student-&gt;name, name);</span><br><span class="line">    student-&gt;age = age;</span><br><span class="line">&#125;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    struct Student s1, s2;</span><br><span class="line">    initializeStudent(&amp;s1, &quot;Tom&quot;, 18);</span><br><span class="line">    initializeStudent(&amp;s2, &quot;Jerry&quot;, 28);</span><br><span class="line">	char *tmp_name;</span><br><span class="line">	int tmp_age;</span><br><span class="line"></span><br><span class="line">	tmp_name=s1.name;</span><br><span class="line">	s1.name=s2.name;</span><br><span class="line">	s2.name=tmp_name;</span><br><span class="line"></span><br><span class="line">	tmp_age=s1.age;</span><br><span class="line">	s1.age=s2.age;</span><br><span class="line">	s2.age=tmp_age;</span><br><span class="line">	</span><br><span class="line">    printf(&quot;s1的姓名: %s 年龄: %d\n&quot;, s1.name, s1.age);</span><br><span class="line">    printf(&quot;s2的姓名: %s 年龄: %d\n&quot;, s2.name, s2.age);</span><br><span class="line">    free(s1.name);</span><br><span class="line">    free(s2.name);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>输出结果：<br> <code>s1的姓名: Jerry 年龄: 28</code><br> <code>s2的姓名: Tom 年龄: 18</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li><code>s1 = s2</code>是一个看起来就很危险的操作。会使得free(s1.name)找不到内存地址而无法释放。</li>
</ol>
<ul>
<li>考点：<br> 结构体、动态分配内存</li>
</ul>
<h2 id="12-你好，我是内存"><a href="#12-你好，我是内存" class="headerlink" title="12. 你好，我是内存"></a>12. 你好，我是内存</h2><p>作为一名合格的C-Coder，一定对内存很敏感吧~来尝试理解这个程序吧！</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct structure &#123;</span><br><span class="line">    int foo;</span><br><span class="line">    union &#123;</span><br><span class="line">      int integer;</span><br><span class="line">      char string[11];</span><br><span class="line">      void *pointer;</span><br><span class="line">    &#125; node;</span><br><span class="line">    short bar;</span><br><span class="line">    long long baz;</span><br><span class="line">    int array[7];</span><br><span class="line">&#125;;</span><br><span class="line">int main(void) &#123;</span><br><span class="line">    int arr[] = &#123;0x590ff23c, 0x2fbc5a4d, 0x636c6557, 0x20656d6f,</span><br><span class="line">                 0x58206f74, 0x20545055, 0x6577202c, 0x6d6f636c,</span><br><span class="line">                 0x6f742065, 0x79695820, 0x4c20756f, 0x78756e69,</span><br><span class="line">                 0x6f724720, 0x5b207075, 0x33323032, 0x7825005d,</span><br><span class="line">                 0x636c6557, 0x64fd6d1d&#125;;</span><br><span class="line">    printf(&quot;%s\n&quot;, ((struct structure *)arr)-&gt;node.string);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>Welcome to XUPT , welcome to Xiyou Linux Group [2023]</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>arr数组内存的分配是连续的。根据小端存储的方式存储数据，之后把数组强制转换为1个指向<code>structure</code>的指针，并且从<code>node</code>内的<code>string</code>下的地址位置<code>8字节</code>，即arr[2]开始读取arr的内存。</li>
<li>arr内存储的是一系列16进制数，这些数以1个字节的大小读取，直到读取到<code>\0</code>结束，并且以字符串的形式输出。</li>
</ol>
<ul>
<li>考点：<br> 结构体大小、小端存储、内存的精细操作</li>
</ul>
<h2 id="13-GNU-Linux-选做"><a href="#13-GNU-Linux-选做" class="headerlink" title="13. GNU&#x2F;Linux (选做)"></a>13. GNU&#x2F;Linux (选做)</h2><p>注：嘿！你或许对Linux命令不是很熟悉，甚至你没听说过Linux。但别担心，这是选做题，了解Linux是加分项，但不了解也不扣分哦！  </p>
<p>你知道cd命令的用法与 &#x2F; . ~ 这些符号的含义吗？<br>请问你还懂得哪些与 GNU&#x2F;Linux 相关的知识呢~</p>
<p><strong>题解：</strong></p>
<ul>
<li>输出结果：</li>
</ul>
<ol>
<li><code>cd /</code>:切换至根目录<br><code>cd .</code>:切换至当前目录<br><code>cd ~</code>:切换至用户主目录  </li>
<li><code>ls</code>:列出目录<br> <code>touch</code>:创建文件<br> <code>clear</code>:清除终端页面<br> ……</li>
</ol>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>西邮 Linux 兴趣小组 2024 纳新面试题</title>
    <url>/2025/10/25/%E8%A5%BF%E9%82%AE%20Linux%20%E5%85%B4%E8%B6%A3%E5%B0%8F%E7%BB%84%202024%20%E7%BA%B3%E6%96%B0%E9%9D%A2%E8%AF%95%E9%A2%98/</url>
    <content><![CDATA[<h1 id="西邮-Linux-兴趣小组-2024-纳新面试题"><a href="#西邮-Linux-兴趣小组-2024-纳新面试题" class="headerlink" title="西邮 Linux 兴趣小组 2024 纳新面试题"></a>西邮 Linux 兴趣小组 2024 纳新面试题</h1><blockquote>
<p>学长寄语：长期以来，西邮 Linux 兴趣小组的面试题以难度之高名扬西邮校内。我们作为出题人也清楚的知道这份试题略有难度。请你动手敲一敲代码。别担心，若有同学能完成一半的题目，就已经十分优秀。其次，相比于题目的答案，我们对你的思路和过程更感兴趣，或许你的答案略有瑕疵，但你正确的思路和对知识的理解足以为你赢得绝大多数的分数。最后，做题的过程也是学习和成长的过程，相信本试题对你更加熟悉地掌握 C 语言一定有所帮助。祝你好运。我们东区逸夫楼 FZ103 见！</p>
</blockquote>
<ul>
<li>本题目只作为西邮 Linux 兴趣小组 2024 纳新面试的有限参考。</li>
<li>为节省版面，本试题的程序源码省去了 <code>#include</code> 指令。</li>
<li>本试题中的程序源码仅用于考察 C 语言基础，不应当作为 C 语言「代码风格」的范例。</li>
<li>所有题目编译并运行于 <em>x86_64 GNU&#x2F;Linux</em> 环境。</li>
</ul>
<h2 id="0-聪明的吗喽"><a href="#0-聪明的吗喽" class="headerlink" title="0. 聪明的吗喽"></a>0. 聪明的吗喽</h2><p>一个小猴子边上有 100 根香蕉，它要走过 50 米才能到家，每次它最多搬 50 根香蕉，（多了就拿不动了），它每走 1 米就要吃掉一根，请问它最多能把多少根香蕉搬到家里。</p>
<p>（提示：他可以把香蕉放下往返走，但是必须保证它每走一米都能有香蕉吃。也可以走到 n 米时，放下一些香蕉，拿着 n 根香蕉走回去重新搬 50 根。）</p>
<p><strong>题解：</strong></p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    int n,f;//n为所持香蕉数，f为需要走的步数</span><br><span class="line">    scanf(&quot;%d %d&quot;,&amp;n,&amp;f);</span><br><span class="line">    int tmp=n/2;//最多搬的香蕉数</span><br><span class="line">    int a=tmp%3;//剩余无法满足走1步消耗3根香蕉的香蕉数</span><br><span class="line">    int b=tmp/3;//在走1步消耗3根香蕉的条件下所走步数</span><br><span class="line">    f-=b;//剩余步数</span><br><span class="line">    printf(&quot;%d&quot;,tmp+a-f);//剩余香蕉</span><br><span class="line">    </span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>输出结果：<br> <code>18</code></p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>由于小猴子一次性最多搬运50根香蕉，而在小猴子所持香蕉数大于50根的情况下，会先带着1批香蕉向前走1步，再拿1根香蕉返回上一个位置，再带着另1批香蕉向前走回刚刚向前走那1步的所在位置，即可以等效于，走1步消耗3根香蕉。</li>
<li>通过计算得知，这样走会剩下2根香蕉，在此刻我们再以该条件走1步，此时，小猴子所持香蕉数已经不大于50根了。我们便可以用走1步消耗1根香蕉的方式走完剩下的路程。</li>
</ol>
<ul>
<li>考点：<br> 通过数学工具来解决实际问题的能力</li>
</ul>
<h2 id="1-西邮Linux欢迎你啊"><a href="#1-西邮Linux欢迎你啊" class="headerlink" title="1. 西邮Linux欢迎你啊"></a>1. 西邮Linux欢迎你啊</h2><p>请解释以下代码的运行结果。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    unsigned int a = 2024;</span><br><span class="line">    for (; a &gt;= 0; a--)</span><br><span class="line">        printf(&quot;%d\n&quot;, printf(&quot;Hi guys! Join Linux - 2%d&quot;, printf(&quot;&quot;)));</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong><br>输出结果：<br><code>Hi guys! Join Linux - 2024</code>(无限循环)</p>
<ul>
<li><p>解释：<br> <code>a</code>为<code>unsigned</code>，即为非负数，故其在自减的过程中，会在减为<code>0</code>时返回到<code>int</code>类型最大值</p>
</li>
<li><p>考点：<br> unsigned、补码</p>
</li>
</ul>
<h2 id="2-眼见不一定为实"><a href="#2-眼见不一定为实" class="headerlink" title="2. 眼见不一定为实"></a>2. 眼见不一定为实</h2><p>输出为什么和想象中不太一样？<br>你了解 <code>sizeof()</code> 和 <code>strlen()</code> 吗？他们的区别是什么？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    char p0[] = &quot;I love Linux&quot;;</span><br><span class="line">    const char *p1 = &quot;I love Linux\0Group&quot;;</span><br><span class="line">    char p2[] = &quot;I love Linux\0&quot;;</span><br><span class="line">    printf(&quot;%d\n%d\n&quot;, strcmp(p0, p1), strcmp(p0, p2));</span><br><span class="line">    printf(&quot;%d\n%d\n&quot;, sizeof(p0) == sizeof(p1), strlen(p0) == strlen(p1));</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>0</code><br> <code>0</code><br> <code>0</code><br> <code>1</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>p0和p2的本质是一样的，p1是一个字符串指针。</li>
<li><code>strcmp</code>函数在比较两个字符串时，碰到了两个<code>&#39;\0&#39;</code>，则停止比较，返回ASCII码。</li>
<li><code>sizeof</code>对指针进行计算，结果为8(64位机)，对字符串进行计算，结果为字符串的长度。</li>
</ol>
<ul>
<li>考点：<br> strcmp和sizeof</li>
</ul>
<h2 id="3-1-1-1-0-0-1"><a href="#3-1-1-1-0-0-1" class="headerlink" title="3. 1.1 - 1.0 !&#x3D; 0.1"></a>3. 1.1 - 1.0 !&#x3D; 0.1</h2><p>为什么会这样，除了下面给出的一种方法，还有什么方法可以避免这个问题？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    float a = 1.0, b = 1.1, ex = 0.1;</span><br><span class="line">    printf(&quot;b - a == ex is %s\n&quot;, (b - a == ex) ? &quot;true&quot; : &quot;false&quot;);</span><br><span class="line">    int A = a * 10, B = b * 10, EX = ex * 10;</span><br><span class="line">    printf(&quot;B - A == EX is %s\n&quot;, (B - A == EX) ? &quot;true&quot; : &quot;false&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br><code>b - a == ex is false</code><br><code>B - A == EX is true</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>a,b,c均为浮点数，其实际值在无限趋近于所给值。故a+b!&#x3D;c。</li>
<li>将对应数乘10，其整数部分具有实际意义，小数部分在转换为int类型时被忽略，正好可以使得相加运算正确。</li>
<li>改进方法：  <ul>
<li>可以使用16进制保存a,b,c这些浮点数，在确定小数后几位的情况下，16进制是精准的。</li>
<li>可以使用误差容忍度，float下为1e-6，double下为1e-12。</li>
</ul>
</li>
</ol>
<ul>
<li>考点：<br> 浮点数、三元运算符</li>
</ul>
<h2 id="4-听说爱用位运算的人技术都不太差"><a href="#4-听说爱用位运算的人技术都不太差" class="headerlink" title="4. 听说爱用位运算的人技术都不太差"></a>4. 听说爱用位运算的人技术都不太差</h2><p>解释函数的原理，并分析使用位运算求平均值的优缺点。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int average(int nums[], int start, int end) &#123;</span><br><span class="line">    if (start == end)</span><br><span class="line">        return nums[start];</span><br><span class="line">    int mid = (start + end) / 2;</span><br><span class="line">    int leftAvg = average(nums, start, mid);</span><br><span class="line">    int rightAvg = average(nums, mid + 1, end);</span><br><span class="line">    return (leftAvg &amp; rightAvg) + ((leftAvg ^ rightAvg) &gt;&gt; 1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li>输出结果：<br> <code>num[5]={1,2,3,4,5}</code><br> <code>3</code></li>
</ul>
<p> -解释：  </p>
<ol>
<li>利用函数递归，将整个数组分成两个部分，分别求平均值，在把结果求平均值，得到最终结果，即分治。</li>
<li>优点：<ul>
<li>避免了整数溢出的问题。</li>
<li>函数整体更整顿。<br>缺点：  </li>
<li>时间复杂度为O(n)  </li>
<li>空间复杂度为O(logn)</li>
</ul>
</li>
</ol>
<ul>
<li>考点：<br> 位运算求平均值、算法的优化思想</li>
</ul>
<h2 id="5-全局还是局部"><a href="#5-全局还是局部" class="headerlink" title="5. 全局还是局部!!!"></a>5. 全局还是局部!!!</h2><p>先思考输出是什么，再动动小手运行下代码，看跟自己想得结果一样不一样 &gt;-&lt;</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int i = 1;</span><br><span class="line">static int j = 15;</span><br><span class="line">int func() &#123;</span><br><span class="line">    int i = 10;</span><br><span class="line">    if (i &gt; 5) i++;</span><br><span class="line">    printf(&quot;i = %d, j = %d\n&quot;, i, j);</span><br><span class="line">    return i % j;</span><br><span class="line">&#125;</span><br><span class="line">int main() &#123;</span><br><span class="line">    int a = func();</span><br><span class="line">    printf(&quot;a = %d\n&quot;, a);</span><br><span class="line">    printf(&quot;i = %d, j = %d\n&quot;, i, j);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>i = 11, j = 15</code><br> <code>a = 11</code><br> <code>i = 1, j = 15</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>程序内有两个函数，func和main，func函数内使用了静态变量<code>j</code>，和一个自动变量<code>i</code>，虽然在函数外部，还有一个变量<code>i</code>，但函数外部的变量<code>i</code>为外部链接的静态变量，他会被一个自动变量所“隐藏”。因此当我们在每次调用这个函数的时候，都会有一个自动变量<code>i</code>，来也匆匆去也匆匆。</li>
<li>而在不调用func函数的时候，自然也不会创建这个自动变量<code>i</code>去覆盖那个静态变量<code>i</code>。于是在第三行打印的时候，<code>i</code>是1，<code>j</code>是15。</li>
</ol>
<ul>
<li>考点：<br> 变量的作用域和存储期</li>
</ul>
<h2 id="6-指针的修罗场：改还是不改，这是个问题"><a href="#6-指针的修罗场：改还是不改，这是个问题" class="headerlink" title="6. 指针的修罗场：改还是不改，这是个问题"></a>6. 指针的修罗场：改还是不改，这是个问题</h2><p>指出以下代码中存在的问题，并帮粗心的学长改正问题。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(int argc, char **argv) &#123;</span><br><span class="line">    int a = 1, b = 2;</span><br><span class="line">    const int *p = &amp;a;</span><br><span class="line">    int * const q = &amp;b;</span><br><span class="line">    *p = 3, q = &amp;a;</span><br><span class="line">    const int * const r = &amp;a;</span><br><span class="line">    *r = 4, r = &amp;b;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong>  </p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line"></span><br><span class="line">int main(int argc, char **argv) &#123;</span><br><span class="line">    int a = 1, b = 2;</span><br><span class="line">    const int *p = &amp;a;</span><br><span class="line">    int * const q = &amp;b;</span><br><span class="line">    *q = 3, p = &amp;a;//q和p的操作恰好相反</span><br><span class="line">    const int * const r = &amp;a;</span><br><span class="line">    //删除了有关r的操作</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>解释：</li>
</ul>
<ol>
<li><code>const</code>在限定一个指针的时候，他和<code>*</code>的先后顺序需要细心关注一下。当<code>*</code>在<code>const</code>的左侧时，<code>const</code>限定了指针的地址；当<code>*</code>在<code>const</code>的右侧时，<code>const</code>限定了指针地址下的值；当<code>*</code>在<code>const</code>中间时，则地址和值都被限定。可以记为”左位右值“。</li>
</ol>
<ul>
<li>考点：<br> const限定符</li>
</ul>
<h2 id="7-物极必反？"><a href="#7-物极必反？" class="headerlink" title="7. 物极必反？"></a>7. 物极必反？</h2><p>你了解 <code>argc</code> 和 <code>argv</code> 吗，这个程序里的 <code>argc</code> 和 <code>argv</code> 是什么？<br>程序输出是什么？解释一下为什么。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(int argc, char *argv[]) &#123;</span><br><span class="line">    while (argc++ &gt; 0);</span><br><span class="line">    int a = 1, b = argc, c = 0;</span><br><span class="line">    if (--a || b++ &amp;&amp; c--)</span><br><span class="line">        for (int i = 0; argv[i] != NULL; i++)</span><br><span class="line">            printf(&quot;argv[%d] = %s\n&quot;, i, argv[i]);</span><br><span class="line">    printf(&quot;a = %d, b = %d, c = %d\n&quot;, a, b, c);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>a = 0, b = -2147483646, c = -1</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>argc为程序命令行的数量，*argv[]内保存着命令行的具体内容。</li>
<li>由于while循环的判断条件和终止条件在同一行内，且其执行的十一条空语句，故一定会执行到argc小于0。由于变量在计算机内以补码形式保存，故为4字节下最小值<code>-2147483648</code>。</li>
<li>在之后又进行了一次自增，把值赋给b后又自增，且在if语句内判断条件时，没有出现短路的情况，故所有操作都执行。</li>
</ol>
<ul>
<li>考点：<br> argc和argv</li>
</ul>
<h2 id="8-指针？数组？数组指针？指针数组？"><a href="#8-指针？数组？数组指针？指针数组？" class="headerlink" title="8. 指针？数组？数组指针？指针数组？"></a>8. 指针？数组？数组指针？指针数组？</h2><p>在主函数中定义如下变量：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    int a[2] = &#123;4, 8&#125;;</span><br><span class="line">    int(*b)[2] = &amp;a;</span><br><span class="line">    int *c[2] = &#123;a, a + 1&#125;;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>说说这些输出分别是什么？  </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">a, a + 1, &amp;a, &amp;a + 1, *(a + 1), sizeof(a), sizeof(&amp;a)</span><br><span class="line">*b, *b + 1, b, b + 1, *(*b + 1), sizeof(*b), sizeof(b)</span><br><span class="line">c, c + 1, &amp;c, &amp;c + 1, **(c + 1), sizeof(c), sizeof(&amp;c)</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<table>
<thead>
<tr>
<th align="left"></th>
<th align="center"></th>
<th align="center"></th>
<th align="center"></th>
<th align="center"></th>
<th align="center"></th>
<th align="right"></th>
</tr>
</thead>
<tbody><tr>
<td align="left">&amp;a[0]</td>
<td align="center">&amp;a[1]</td>
<td align="center">数组a的地址</td>
<td align="center">数组a末尾的地址</td>
<td align="center">8</td>
<td align="center">8</td>
<td align="right">8</td>
</tr>
<tr>
<td align="left">&amp;a[0]</td>
<td align="center">&amp;a[1]</td>
<td align="center">数组a的地址</td>
<td align="center">数组a末尾的地址</td>
<td align="center">8</td>
<td align="center">8</td>
<td align="right">8</td>
</tr>
<tr>
<td align="left">&amp;&amp;a[0]</td>
<td align="center">&amp;&amp;a[1]</td>
<td align="center">数组c的地址</td>
<td align="center">数组c末尾的地址</td>
<td align="center">8</td>
<td align="center">8</td>
<td align="right">8</td>
</tr>
</tbody></table>
<ul>
<li>解释：</li>
</ul>
<ol>
<li><code>a[2]</code>是一个数组，<code>(*b)[2]</code>是一个指向含有2个元素的数组的指针，<code>*c[2]</code>是一个包含两个指针的数组。</li>
<li><code>&amp;a</code>的意思是整个数组的地址，而<code>&amp;a[0]</code>是指数组的入口地址。</li>
<li><code>sizeof</code>是计算其所占内存的大小。</li>
</ol>
<ul>
<li>考点：<br> 数组，指针数组，数组指针、sizeof</li>
</ul>
<h2 id="9-嘻嘻哈哈，好玩好玩"><a href="#9-嘻嘻哈哈，好玩好玩" class="headerlink" title="9. 嘻嘻哈哈，好玩好玩"></a>9. 嘻嘻哈哈，好玩好玩</h2><p>在宏的魔法下，数字与文字交织，猜猜结果是什么？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define SQUARE(x) x *x</span><br><span class="line">#define MAX(a, b) (a &gt; b) ? a : b;</span><br><span class="line">#define PRINT(x) printf(&quot;嘻嘻，结果你猜对了吗，包%d滴\n&quot;, x);</span><br><span class="line">#define CONCAT(a, b) a##b</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    int CONCAT(x, 1) = 5;</span><br><span class="line">    int CONCAT(y, 2) = 3;</span><br><span class="line">    int max = MAX(SQUARE(x1 + 1), SQUARE(y2))</span><br><span class="line">    PRINT(max)</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>嘻嘻，结果你猜对了吗，包11滴</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>CONCAT的意思是，把a和b直接拼接起来，作为一个标识符。因此<code>x1=5</code>，<code>y2=3</code>。</li>
<li>在SQUARE中，对<code>x1+1</code>的操作为<code>x1+1*x1+1</code>，对<code>y2</code>的操作为<code>y2*y2</code>，取最大值赋值给max，打印max。</li>
</ol>
<ul>
<li>考点：<br> 宏</li>
</ul>
<h2 id="10-我写的排序最快"><a href="#10-我写的排序最快" class="headerlink" title="10. 我写的排序最快"></a>10. 我写的排序最快</h2><p>写一个 your_sort 函数，要求不能改动 main 函数里的代码，对 arr1 和 arr2 两个数组进行升序排序并剔除相同元素，最后将排序结果放入 result 结构体中。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    int arr1[] = &#123;2, 3, 1, 3, 2, 4, 6, 7, 9, 2, 10&#125;;</span><br><span class="line">    int arr2[] = &#123;2, 1, 4, 3, 9, 6, 8&#125;;</span><br><span class="line">    int len1 = sizeof(arr1) / sizeof(arr1[0]);</span><br><span class="line">    int len2 = sizeof(arr2) / sizeof(arr2[0]);</span><br><span class="line"></span><br><span class="line">    result result;</span><br><span class="line">    your_sort(arr1, len1, arr2, len2, &amp;result);</span><br><span class="line">    for (int i = 0; i &lt; result.len; i++) &#123;</span><br><span class="line">        printf(&quot;%d &quot;, result.arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    free(result.arr);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;   </span><br><span class="line">#include&lt;stdlib.h&gt; </span><br><span class="line"></span><br><span class="line">typedef struct</span><br><span class="line">&#123;</span><br><span class="line">    int* arr;</span><br><span class="line">    int len;</span><br><span class="line">&#125;result;</span><br><span class="line"></span><br><span class="line">void your_sort(int arr1[],int len1,int arr2[],int len2,result* res);</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    int arr1[] = &#123;2, 3, 1, 3, 2, 4, 6, 7, 9, 2, 10&#125;;</span><br><span class="line">    int arr2[] = &#123;2, 1, 4, 3, 9, 6, 8&#125;;</span><br><span class="line">    int len1 = sizeof(arr1) / sizeof(arr1[0]);</span><br><span class="line">    int len2 = sizeof(arr2) / sizeof(arr2[0]);</span><br><span class="line"></span><br><span class="line">    result result;</span><br><span class="line">    your_sort(arr1, len1, arr2, len2, &amp;result);</span><br><span class="line">    for (int i = 0; i &lt; result.len; i++) &#123;</span><br><span class="line">        printf(&quot;%d &quot;, result.arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    free(result.arr);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void your_sort(int arr1[],int len1,int arr2[],int len2,result* res)</span><br><span class="line">&#123;</span><br><span class="line">    res-&gt;arr=(int*)malloc(sizeof(int)*(len1+len2));</span><br><span class="line">    int i,j;</span><br><span class="line"></span><br><span class="line">    for(i=0;i&lt;len1;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        res-&gt;arr[i]=arr1[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    for(i=0;i&lt;len2;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        res-&gt;arr[i+len1]=arr2[i];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    res-&gt;len=len1+len2;</span><br><span class="line"></span><br><span class="line">    for(i=0;i&lt;res-&gt;len-1;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        for(j=0;j&lt;=res-&gt;len-1-i;j++)</span><br><span class="line">        &#123;</span><br><span class="line">            if(res-&gt;arr[j]&gt;res-&gt;arr[j+1])</span><br><span class="line">            &#123;</span><br><span class="line">                int tmp=res-&gt;arr[j];</span><br><span class="line">                res-&gt;arr[j]=res-&gt;arr[j+1];   </span><br><span class="line">                res-&gt;arr[j+1]=tmp;   </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    int new_size=1;</span><br><span class="line"></span><br><span class="line">    for(i=1;i&lt;res-&gt;len;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        if(res-&gt;arr[i]!=res-&gt;arr[i-1])</span><br><span class="line">        &#123;</span><br><span class="line">            res-&gt;arr[new_size]=res-&gt;arr[i];</span><br><span class="line">            new_size++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    res-&gt;arr=(int*)realloc(res-&gt;arr,sizeof(int)*new_size);</span><br><span class="line">    res-&gt;len=new_size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>输出结果：<br> <code>1 2 3 4 6 7 8 9 10 </code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>首先定义了一个结构体，并且把这个结构体命名为<code>result</code>。</li>
<li>在<code>your_sort</code>函数内，分别进行了拼接，排序，去重三个步骤，并且使用了动态分配内存。这里的排序我使用的是冒泡排序。</li>
</ol>
<ul>
<li>考点：<br> 结构体访问、动态分配内存、拼接，排序，去重</li>
</ul>
<h2 id="11-猜猜我是谁"><a href="#11-猜猜我是谁" class="headerlink" title="11. 猜猜我是谁"></a>11. 猜猜我是谁</h2><p>在指针的迷宫中，五个数字化身为神秘的符号，等待被逐一揭示。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    void *a[] = &#123;(void *)1, (void *)2, (void *)3, (void *)4, (void *)5&#125;;</span><br><span class="line">    printf(&quot;%d\n&quot;, *((char *)a + 1));</span><br><span class="line">    printf(&quot;%d\n&quot;, *(int *)(char *)a + 1);</span><br><span class="line">    printf(&quot;%d\n&quot;, *((int *)a + 2));</span><br><span class="line">    printf(&quot;%lld\n&quot;, *((long long *)a + 3));</span><br><span class="line">    printf(&quot;%d\n&quot;, *((short *)a + 4));</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>0</code><br> <code>2</code><br> <code>2</code><br> <code>4</code><br> <code>2</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li><code>*a[]</code>是一个指针数组。在<code>64</code>位机中地址为<code>8</code>字节，且为小端存储。</li>
<li>先转换为<code>char*</code>类型，后移动<code>1</code>个字节，解引用的结果为<code>0</code>。</li>
<li>先转换为<code>char*</code>类型，后转换为<code>int*</code>类型，对其<code>4</code>字节下的地址解引用，结果为<code>1</code>，<code>1+1=2</code>。</li>
<li>先转换为<code>long long*</code>类型，后移动<code>8*3=24</code>个字节，解引用的结果为<code>4</code>。</li>
<li>先转换为<code>short*</code>类型，后移动<code>2*4=8</code>字节，解引用的结果为<code>2</code>。</li>
</ol>
<ul>
<li>考点：<br> 指针的类型转换和运算</li>
</ul>
<h2 id="12-结构体变小写奇遇记"><a href="#12-结构体变小写奇遇记" class="headerlink" title="12. 结构体变小写奇遇记"></a>12. 结构体变小写奇遇记</h2><p>计算出 <code>Node</code> 结构体的大小，并解释以下代码的运行结果。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">union data &#123;</span><br><span class="line">    int a;</span><br><span class="line">    double b;</span><br><span class="line">    short c;</span><br><span class="line">&#125;;</span><br><span class="line">typedef struct node &#123;</span><br><span class="line">    long long a;</span><br><span class="line">    union data b;</span><br><span class="line">    void (*change)( struct node *n);</span><br><span class="line">    char string[0];</span><br><span class="line">&#125; Node;</span><br><span class="line">void func(Node *node) &#123;</span><br><span class="line">    for (size_t i = 0; node-&gt;string[i] != &#x27;\0&#x27;; i++)</span><br><span class="line">        node-&gt;string[i] = tolower(node-&gt;string[i]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    const char *s = &quot;WELCOME TO XIYOULINUX_GROUP!&quot;;</span><br><span class="line">    Node *P = (Node *)malloc(sizeof(Node) + (strlen(s) + 1) * sizeof(char));</span><br><span class="line">    strcpy(P-&gt;string, s);</span><br><span class="line">    P-&gt;change = func;</span><br><span class="line">    P-&gt;change(P);</span><br><span class="line">    printf(&quot;%s\n&quot;, P-&gt;string);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>welcome to xiyoulinux_group!</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>首先进行内存对齐，data为<code>24</code>字节，Node为<code>40</code>字节。</li>
<li>在main函数内对P进行动态内存分配，大小为<code>40+30=70</code>字节。</li>
<li><code>P-&gt;change = func</code>把change函数指针指向了<code>func</code>，<code>P-&gt;change(P)</code>调用了指向的<code>func</code>函数。</li>
<li>在其中还有一个字符串变为小写的tolower()函数，使得结果最后均为小写。</li>
</ol>
<ul>
<li>考点：<br> 结构体和共用体内存对齐、动态内存分配、字符串常量的存储方式</li>
</ul>
<h2 id="13-GNU-Linux-选做"><a href="#13-GNU-Linux-选做" class="headerlink" title="13. GNU&#x2F;Linux (选做)"></a>13. GNU&#x2F;Linux (选做)</h2><p>注：嘿！你或许对Linux命令不是很熟悉，甚至没听说过Linux。<br>但别担心，这是选做题，了解Linux是加分项，不了解也不扣分哦！</p>
<ol>
<li>你知道 <code>ls</code> 命令的用法与 <code>/</code> <code>.</code> <code>~</code> 这些符号的含义吗？</li>
<li>你知道 Linux 中权限 <code>rwx</code> 的含义吗？</li>
<li>请问你还懂得哪些与 GNU&#x2F;Linux 相关的知识呢？</li>
</ol>
<p><strong>题解：</strong></p>
<ul>
<li>输出结果：</li>
</ul>
<ol>
<li><code>ls</code>:列出文件<br><code>ls /</code>:列出根目录下的所有目录<br><code>ls .</code>:列出当前目录下的所有目录<br><code>ls ~</code>:列出用户主目录下的所有目录  </li>
<li><code>r</code>:读取权限<br><code>w</code>:写入权限<br><code>x</code>:执行权限</li>
<li><code>cd</code>:更改目录<br> <code>touch</code>:创建文件<br> <code>clear</code>:清除终端页面<br> ……</li>
</ol>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>C</tag>
      </tags>
  </entry>
  <entry>
    <title>西邮 Linux 兴趣小组 2025 纳新面试题</title>
    <url>/2025/10/23/%E8%A5%BF%E9%82%AE%20Linux%20%E5%85%B4%E8%B6%A3%E5%B0%8F%E7%BB%84%202025%20%E7%BA%B3%E6%96%B0%E9%9D%A2%E8%AF%95%E9%A2%98/</url>
    <content><![CDATA[<h1 id="西邮-Linux-兴趣小组-2025-纳新面试题"><a href="#西邮-Linux-兴趣小组-2025-纳新面试题" class="headerlink" title="西邮 Linux 兴趣小组 2025 纳新面试题"></a>西邮 Linux 兴趣小组 2025 纳新面试题</h1><blockquote>
<p>学长寄语：长期以来，西邮 Linux 兴趣小组的面试题以难度之高名扬西邮校内。我们作为出题人也清楚地知道这份试题略有难度。请你动手敲一敲代码。别担心，若有同学能完成一半的题目，就已经十分优秀。其次，相比于题目的答案，我们对你的思路和过程更感兴趣，或许你的答案略有瑕疵，但你正确的思路和对知识的理解足以为你赢得绝大多数的分数。最后，做题的过程也是学习和成长的过程，相信本试题对你更加熟悉地掌握 C 语言一定有所帮助。祝你好运。我们东区逸夫楼 FZ103 见！</p>
</blockquote>
<ul>
<li>本题目只作为西邮 Linux 兴趣小组 2025 纳新面试的有限参考。</li>
<li>为节省版面，本试题的程序源码省去了 <code>include</code> 指令。</li>
<li>本试题中的程序源码仅用于考察 C 语言基础，不应当作为 C 语言「代码风格」的范例。</li>
<li>所有题目编译并运行于 <em>x86_64 GNU&#x2F;Linux</em> 环境。</li>
</ul>
<h2 id="0-拼命的企鹅"><a href="#0-拼命的企鹅" class="headerlink" title="0. 拼命的企鹅"></a>0. 拼命的企鹅</h2><p>一只企鹅在爬山，每隔一段路都会遇到一块石头。第 1 块石头重量是 <code>a</code>，每往上走一段路，石头重量就会变成上一段的平方。企鹅可以选择把某些石头捡起来，最后把捡到的石头重量相乘。</p>
<p>它怎样捡石头，才能得到重量乘积恰好是 <code>a</code> 的 <code>b</code> 次方的石头？（比如 <code>b = 173</code> 时， 要捡哪些石头？）</p>
<p><strong>题解：</strong></p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line"></span><br><span class="line">int main()</span><br><span class="line">&#123;</span><br><span class="line">    int b;</span><br><span class="line">    scanf(&quot;%d&quot;,&amp;b);</span><br><span class="line">    int printpoint=1;//insert &#x27;,&#x27;.</span><br><span class="line">    int point=1;//the situation of the key stone.</span><br><span class="line"></span><br><span class="line">    while(b&gt;0)</span><br><span class="line">    &#123;</span><br><span class="line">        if(b&amp;1)//get the key.</span><br><span class="line">        &#123;</span><br><span class="line">            if(!printpoint)</span><br><span class="line">            &#123;</span><br><span class="line">                printf(&quot;,&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">            printf(&quot;%d&quot;,point);</span><br><span class="line">            printpoint=0;</span><br><span class="line">        &#125;</span><br><span class="line">        b&gt;&gt;=1;//delete the previous.</span><br><span class="line">        point++;//change the situation.</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>输出结果：<br> 当<code>b = 173</code>时，结果为：<code>1,3,4,6,8</code><br> 当<code>b = 65</code>时，结果为：<code>1,7</code></p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>由题意可知，捡到的石头重量相乘，即对应石头指数相加，而我们可以将石头的重量：${a^n}$可以写作$a^{2^n}$，于是在相乘的过程中，可以等效转换为是2的n次幂的相加，即2进制转10进制。</li>
<li>通过每次对2进制下的<code>1</code>读取下标，便可以输出<code>1</code>的位置。</li>
</ol>
<ul>
<li>考点：<br> 2进制与10进制转换、通过数学工具来解决实际问题的能力</li>
</ul>
<h2 id="1-西邮Linux欢迎你啊"><a href="#1-西邮Linux欢迎你啊" class="headerlink" title="1. 西邮Linux欢迎你啊"></a>1. 西邮Linux欢迎你啊</h2><p>请解释以下代码的运行结果。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    if (printf(&quot;Hi guys! &quot;) || printf(&quot;Xiyou Linux &quot;)) &#123;</span><br><span class="line">        printf(&quot;%d\n&quot;, printf(&quot;Welcome to Xiyou Linux 2%d&quot;, printf(&quot;&quot;)));</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br><code>Hi guys! Welcome to Xiyou Linux 2025</code></p>
</li>
<li><p>解释：  </p>
<ol>
<li><p>if语句中执行printf函数，其返回值：打印的字符长度<code>10</code>，作为判断if语句成立的条件。由于两个printf的关系为<strong>或</strong>，第一个printf函数的返回值已经使if语句成立，故不执行第二个printf函数。结果为：<code>Hi guys! </code></p>
</li>
<li><p>在if语句内，printf函数嵌套了其他printf函数，从内向外依次输出，对应的返回值作为输出内容，以<code>%d</code>的形式输出。结果为：<code>Welcome to Xiyou Linux 2025</code>。</p>
</li>
</ol>
</li>
<li><p>考点：<br>“短路”、printf函数返回值</p>
</li>
</ul>
<h2 id="2-可以和-0-组一辈子字符串吗？"><a href="#2-可以和-0-组一辈子字符串吗？" class="headerlink" title="2. 可以和 \0 组一辈子字符串吗？"></a>2. 可以和 <code>\0</code> 组一辈子字符串吗？</h2><p>你能找到成功打印的语句吗？你能看出每个 <code>if</code> 中函数的返回值吗？这些函数各有什么特点？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    char p1[] = &#123; &#x27;W&#x27;, &#x27;e&#x27;, &#x27;l&#x27;, &#x27;c&#x27;, &#x27;o&#x27;, &#x27;m&#x27;, &#x27;e&#x27;, &#x27;\0&#x27; &#125;;</span><br><span class="line">    char p2[] = &quot;Join us\0&quot;, p4[] = &quot;xiyou_linux_group&quot;;</span><br><span class="line">    const char* p3 = &quot;Xiyou Linux Group\0\n2025\0&quot;;</span><br><span class="line">    if (strcmp(p1, p2)) &#123;</span><br><span class="line">        printf(&quot;%s to %s!\n&quot;, p1, p2);</span><br><span class="line">    &#125;</span><br><span class="line">    if (strlen(p3) &gt; sizeof(p3)) &#123;</span><br><span class="line">        printf(&quot;%s&quot;, p3);</span><br><span class="line">    &#125;</span><br><span class="line">    if (sizeof(p1) == sizeof(p2)) &#123;</span><br><span class="line">        printf(&quot;%s&quot;, p4);</span><br><span class="line">    &#125;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>Welcome to Join us!</code><br> <code>Xiyou Linux Group</code></p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>第一个if语句判断中，<code>strcmp</code>函数进行了两个字符串的比较，由于<code>&#39;w&#39;</code>比<code>&#39;J&#39;</code>在字符表中更靠前，其ASCII码差值为正(非0)，则if语句成立，执行<code>printf</code>函数。</li>
<li><code>sizeof(p3)</code>是对指针的大小进行计算，则其值为<code>8</code>，<code>strlen(p3)</code>是对字符串整体的大小进行计算，在第一个<code>&#39;\0&#39;</code>停止且不包括<code>&#39;\0&#39;</code>，故其值为<code>17</code>，比较结果在if语句中成立，执行<code>printf</code>函数。</li>
<li>两个字符串的大小相等，故其if语句成立，执行<code>printf</code>函数。</li>
</ol>
<ul>
<li>考点：<br> sizeof和strlen的使用</li>
</ul>
<h2 id="3-数学没问题，浮点数有鬼"><a href="#3-数学没问题，浮点数有鬼" class="headerlink" title="3. 数学没问题，浮点数有鬼"></a>3. 数学没问题，浮点数有鬼</h2><p>这个程序的输出是什么？解释为什么会这样？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    float a1 = 0.3, b1 = 6e-1, sum1 = 0.9;</span><br><span class="line">    printf(&quot;a1 + b1 %s sum1\n&quot;, (a1 + b1 == sum1) ? &quot;==&quot; : &quot;!=&quot;);</span><br><span class="line">    float a2 = 0x0.3p0, b2 = 0x6p-4, sum2 = 0x0.9p0;</span><br><span class="line">    printf(&quot;a2 + b2 %s sum2\n&quot;, (a2 + b2 == sum2) ? &quot;==&quot; : &quot;!=&quot;);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>a1 + b1 != sum1</code><br> <code>a2 + b2 == sum2</code></p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>浮点数本身并不精确，实际值在对应精度下无限趋近于所给值。故不相等。  </li>
<li>三个数均为16进制数，其实际值即所给值。<br> 16进制转10进制：若无小数，则<code>10进制数=整数*2的n次方(n为p后数值)</code></li>
</ol>
<ul>
<li>考点：<br> 16进制转10进制、浮点数</li>
</ul>
<h2 id="4-不合群的数字"><a href="#4-不合群的数字" class="headerlink" title="4. 不合群的数字"></a>4. 不合群的数字</h2><p>在一个数组中，所有数字都出现了偶数次，只有两个数字出现了奇数次，请聪明的你帮我看看以下的代码是如何找到这两个数字的呢？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void findUndercoverIDs(int nums[], int size) &#123;</span><br><span class="line">    int xorAll = 0,id_a = 0,id_b = 0;</span><br><span class="line">    for (int i = 0; i &lt; size; i++) &#123;</span><br><span class="line">        xorAll ^= nums[i];</span><br><span class="line">    &#125;</span><br><span class="line">    int diffBit = xorAll &amp; -xorAll;</span><br><span class="line">    for (int i = 0; i &lt; size; i++) &#123;</span><br><span class="line">        if(nums[i] &amp; diffBit)&#123;</span><br><span class="line">            id_a ^= nums[i];</span><br><span class="line">        &#125; else &#123;</span><br><span class="line">            id_b ^= nums[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;These nums are %d %d\n&quot;, id_a, id_b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> 两个出现奇数次的数字</p>
</li>
<li><p>解释：<br> 整体函数巧妙地利用了位运算，得到了出现奇数次的两个数字。</p>
</li>
</ul>
<ol>
<li>根据异或的特点，即两个相同的数字的结果为0，进行异或运算。如若有两个数字出现了偶数次，便会在异或中被消除，于是我们得到了两个出现奇数次的数字异或的结果，即<code>xorAll</code>。</li>
<li>对<code>xorAll</code>与其本身的相反数(反码+1，即补码)进行位与运算。于是我们便得到了最低位的<code>1</code>。</li>
<li>对数组进行遍历，并在if语句条件判断中进行位与操作，如果结果成立，则把对应值赋给<code>id_a</code>，否则把对应值给<code>id_b</code>。这个操作更精妙之处，在于通过每次的异或，不仅仅进行赋值操作，更去除了出现偶数次的数。</li>
<li>分别输出两个出现奇数次的数字。</li>
</ol>
<ul>
<li>考点：<br> 位运算的进阶操作</li>
</ul>
<h2 id="5-会一直循环吗？"><a href="#5-会一直循环吗？" class="headerlink" title="5. 会一直循环吗？"></a>5. 会一直循环吗？</h2><p>你了解 <code>argc</code> 和 <code>argv</code> 吗，程序的输出是什么？为什么会这样？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main(int argc, char* argv[]) &#123;</span><br><span class="line">    printf(&quot;argc = %d\n&quot;, argc);</span><br><span class="line">    while (argc++ &gt; 0) &#123;</span><br><span class="line">        if(argc &lt; 0)&#123;</span><br><span class="line">            printf(&quot;argv[0] %s\n&quot;, argv[0]);</span><br><span class="line">            break;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;argc = %d\n&quot;, argc);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> argc &#x3D; 1<br> argv[0] .&#x2F;文件<br> argc &#x3D; -2147483648  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>打印了argc的数量，即第一次我们调用程序时，输入的命令行(.&#x2F;文件名称)</li>
<li>在while循环内进行了argc的判断与自增，并且在其中加入了argc的判断。argc在不断自增的过程中，由于其本身便是int类型，故会增加到一定大小后，变成负数。当变为负数时，if语句成立，输出argv[0]，跳出while循环，不会argc的自增~</li>
<li>输出argc的大小。</li>
</ol>
<ul>
<li>考点：<br> argc与argv[]的含义</li>
</ul>
<h2 id="6-const-与指针：谁能动，谁不能动？"><a href="#6-const-与指针：谁能动，谁不能动？" class="headerlink" title="6. const 与指针：谁能动，谁不能动？"></a>6. const 与指针：谁能动，谁不能动？</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct P &#123;</span><br><span class="line">    int x;</span><br><span class="line">    const int y;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    struct P p1 = &#123; 10, 20 &#125;, p2 = &#123; 30, 40 &#125;;</span><br><span class="line">    const struct P p3 = &#123; 50, 60 &#125;;</span><br><span class="line">    struct P* const ptr1 = &amp;p1;</span><br><span class="line">    const struct P* ptr2 = &amp;p2;</span><br><span class="line">    const struct P* const ptr3 = &amp;p3;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>说说下列操作是否合法，并解释原因：</p>
<p><code>ptr1-&gt;x = 100</code>, <code>ptr2-&gt;x = 300</code>, <code>ptr3-&gt;x = 500</code></p>
<p><code>ptr1-&gt;y = 200</code>, <code>ptr1 = &amp;p2</code>, <code>ptr2-&gt;y = 400</code></p>
<p><code>ptr2 = &amp;p1</code>, <code>ptr3-&gt;y = 600</code>, <code>ptr3 = &amp;p1</code></p>
<p><strong>题解：</strong><br>输出结果：</p>
<table>
<thead>
<tr>
<th align="left"></th>
<th align="center"></th>
<th align="right"></th>
</tr>
</thead>
<tbody><tr>
<td align="left">合法</td>
<td align="center">不合法</td>
<td align="right">不合法</td>
</tr>
<tr>
<td align="left">不合法</td>
<td align="center">不合法</td>
<td align="right">不合法</td>
</tr>
<tr>
<td align="left">合法</td>
<td align="center">不合法</td>
<td align="right">不合法</td>
</tr>
</tbody></table>
<ul>
<li>解释：</li>
</ul>
<ol>
<li>首先定义了一个结构体，在结构体内有两个变量，<code>x</code>是<code>int</code>类型，<code>y</code>是<code>const int</code>类型，即无法通过指针改变<code>y</code>的值。</li>
<li><code>p1</code>指针锁定了他指向的位置，<code>p2</code>指针锁定了他指向的内容，<code>p3</code>指针锁定了他指向的位置和内容。三个指针的区别就在于<code>const</code>的修饰。<code>*</code>在<code>const</code>前，在<code>const</code>后，在两个<code>const</code>中间。鉴于博主本人在学习此处时容易混淆，于是自己想了个小口诀，针对<code>*</code>在<code>const</code>的相对位置，“左位右值”（高中记物理二级结论的DNA动了）。供大家参考。</li>
</ol>
<ul>
<li>考点：<br> const限定符、结构体</li>
</ul>
<h2 id="7-指针！数组"><a href="#7-指针！数组" class="headerlink" title="7. 指针！数组!"></a>7. 指针！数组!</h2><p>在主函数中定义如下变量:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    int a[3] = &#123; 2, 4, 8 &#125;;</span><br><span class="line">    int(*b)[3] = &amp;a;</span><br><span class="line">    int* c[3] = &#123; a, a + 1, a + 2 &#125;;</span><br><span class="line">    int (*f1(int))(int*, int);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>说说这几个表达式的输出分别是什么？</p>
<p><code>a</code>, <code>*b</code>, <code>*b + 1</code>, <code>b</code>, <code>b + 1</code>, <code>* (*b + 1)</code>, <code>c</code>, <code>sizeof(a)</code>, <code>sizeof(b)</code>, <code>sizeof(&amp;a)</code>, <code>sizeof(f1)</code></p>
<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>a</code>:a[0]的地址<br> <code>*b</code>:a[0]的地址<br> <code>*b + 1</code>:a[1]的地址<br> <code>b</code>:a的地址<br> <code>b + 1</code>:a末尾的地址<br> <code>* (*b + 1)</code>:4<br> <code>c</code>:a[0]的地址的地址<br> <code>sizeof(a)</code>:3(元素个数)*4(int字节大小)&#x3D;12<br> <code>sizeof(b)</code>:8<br> <code>sizeof(&amp;a)</code>:8<br> <code>sizeof(f1)</code>:编译错误</p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li><code>a[3]</code>是一个含有三个<code>int</code>类型的数组<br> <code>(*b)[3]</code>是一个指向含有三个<code>int</code>元素类型的数组的指针<br> <code>* c[3]</code>是一个含有三个指向<code>int</code>指针类型的数组<br> <code>f1</code>是一个函数，他接受一个<code>int</code>类型的参数，返回为函数指针，该函数指针又可以接受一个<code>int</code>类型指针与<code>int</code>类型的参数，返回为<code>int</code>类型</li>
<li><code>sizeof</code>函数不可以操作函数。</li>
</ol>
<ul>
<li>考点：<br> sizeof的使用、数组，数组指针，指针数组的区别、函数指针（十分神奇的一项操作）</li>
</ul>
<h2 id="8-全局还是局部！！！"><a href="#8-全局还是局部！！！" class="headerlink" title="8. 全局还是局部！！！"></a>8. 全局还是局部！！！</h2><p>观察程序输出，思考为什么？</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int g;</span><br><span class="line">int func() &#123;</span><br><span class="line">    static int j = 98;</span><br><span class="line">    j += g;</span><br><span class="line">    return j;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    g += 3;</span><br><span class="line">    char arr[6] = &#123;&#125;;</span><br><span class="line">    arr[1] = func();</span><br><span class="line">    arr[0] = func();</span><br><span class="line">    arr[2] = arr[3] = func() + 1;</span><br><span class="line">    arr[4] = func() + 1;</span><br><span class="line">    printf(&quot;%s linux\n&quot;,arr);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>hello linux</code></p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li><code>arr[0]</code>:h<br> <code>arr[1]</code>:e<br> <code>arr[2]</code>:l<br> <code>arr[3]</code>:l<br> <code>arr[4]</code>:$o$  </li>
<li>在调用函数的过程中，每次<code>j</code>变量被增加，返回值被赋给了对应数组元素。</li>
<li><code>static</code>静态局部变量下，初始化只进行一次，且j变量存储在数据段。</li>
</ol>
<ul>
<li>考点：<br> 静态局部变量、ASCII码</li>
</ul>
<h2 id="9-宏函数指针"><a href="#9-宏函数指针" class="headerlink" title="9. 宏函数指针"></a>9. 宏函数指针</h2><p>观察程序结果，说说程序运行的过程：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define CALL_MAIN(main, x) (*(int (*)(int))*main)(x);</span><br><span class="line">#define DOUBLE(x) 2 * x</span><br><span class="line">int (*registry[1])(int);</span><br><span class="line">int main(int argc) &#123;</span><br><span class="line">    if (argc &gt; 2e3) return 0;</span><br><span class="line">    printf(&quot;%d &quot;, argc + 1);</span><br><span class="line">    *registry = (int(*)(int))main;</span><br><span class="line">    CALL_MAIN(registry, DOUBLE(argc + 1));</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>2 4 8 16 32 64 128 256 512 1024 </code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>首先，(int (*)(int))是一个函数指针，他保存的是他所指函数的地址，并且在CALL_MAIN内，把main函数强制转换为一个函数指针，由于这个函数指针的地址就是他本身，main函数，即他在指向自己。在这个函数指针内，他接受一个int类型的变量，返回int类型，这个函数也恰好符合main函数的声明。</li>
<li>由于这个函数在不断的递归自己，则需要一个跳出递归的条件，即在宏定义处的DOUBLE起到了作用。通过每次的运算改变argc的值，并且加入if语句来判断是否结束该函数，便成功的解决了无限递归的问题。</li>
<li>resistry在声明时也是个函数指针，但是他本身是一个存储1个元素的数组，元素类型为int类型的指针。于是在resistry[0]内，保存的是指向main函数的函数指针。</li>
<li>在CALL_MAIN处，调用main函数，进行argc的运算，实现函数递归。</li>
</ol>
<ul>
<li>考点：<br> 宏定义、函数指针、函数指针数组</li>
</ul>
<h2 id="10-拼接-排序-去重"><a href="#10-拼接-排序-去重" class="headerlink" title="10. 拼接 排序 去重"></a>10. 拼接 排序 去重</h2><p>本题要求你编写以下函数，不能改动 <code>main</code> 函数里的代码。实现对 <code>arr1</code> 和 <code>arr2</code> 的拼接、排序和去重。你需要自行定义 <code>result</code> 结构体并使用 <code>malloc</code> 手动开辟内存。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main() &#123;</span><br><span class="line">    int arr1[] = &#123; 6, 1, 2, 1, 9, 1, 3, 2, 6, 2 &#125;;</span><br><span class="line">    int arr2[] = &#123; 4, 2, 2, 1, 6, 2 &#125;;</span><br><span class="line">    int len1 = sizeof(arr1) / sizeof(arr1[0]);</span><br><span class="line">    int len2 = sizeof(arr2) / sizeof(arr2[0]);</span><br><span class="line"></span><br><span class="line">    struct result result;</span><br><span class="line">    your_concat(arr1, len1, arr2, len2, result);</span><br><span class="line">    print_result(result);</span><br><span class="line">    your_sort(result);</span><br><span class="line">    print_result(result);</span><br><span class="line">    your_dedup(result);</span><br><span class="line">    print_result(result);</span><br><span class="line">    free(result.arr);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li>代码：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;stdlib.h&gt;</span><br><span class="line">struct result</span><br><span class="line">&#123;</span><br><span class="line">    int* arr;</span><br><span class="line">    int size;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">void your_concat(int arr1[],int len1,int arr2[],int len2,struct result* res);</span><br><span class="line">void print_result(struct result res);</span><br><span class="line">void your_sort(struct result* res);</span><br><span class="line">void sort(int arr[],int low,int high);//it&#x27;s a qsort.</span><br><span class="line">void swap(int* a,int* b);</span><br><span class="line">void your_dedup(struct result* res);</span><br><span class="line"></span><br><span class="line">int main() &#123;</span><br><span class="line">    int arr1[] = &#123; 6, 1, 2, 1, 9, 1, 3, 2, 6, 2 &#125;;</span><br><span class="line">    int arr2[] = &#123; 4, 2, 2, 1, 6, 2 &#125;;</span><br><span class="line">    int len1 = sizeof(arr1) / sizeof(arr1[0]);</span><br><span class="line">    int len2 = sizeof(arr2) / sizeof(arr2[0]);</span><br><span class="line"></span><br><span class="line">    struct result result;</span><br><span class="line">    your_concat(arr1, len1, arr2, len2, &amp;result);</span><br><span class="line">    print_result(result);</span><br><span class="line">    your_sort(&amp;result);</span><br><span class="line">    print_result(result);</span><br><span class="line">    your_dedup(&amp;result);</span><br><span class="line">    print_result(result);</span><br><span class="line">    free(result.arr);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void your_concat(int arr1[],int len1,int arr2[],int len2,struct result* res)</span><br><span class="line">&#123;</span><br><span class="line">    res-&gt;arr=(int*)malloc((len1+len2)*sizeof(int));</span><br><span class="line">    int i;</span><br><span class="line"></span><br><span class="line">    for(i=0;i&lt;len1;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        res-&gt;arr[i]=arr1[i];</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    for(i=0;i&lt;len2;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        res-&gt;arr[len1+i]=arr2[i];</span><br><span class="line">    &#125;</span><br><span class="line">    res-&gt;size=len1+len2;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void print_result(struct result res)</span><br><span class="line">&#123;</span><br><span class="line">    int i;</span><br><span class="line">    for(i=0;i&lt;res.size;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        printf(&quot;%d &quot;,res.arr[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    printf(&quot;\n&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void your_sort(struct result* res)</span><br><span class="line">&#123;</span><br><span class="line">    sort(res-&gt;arr,0,res-&gt;size-1);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void sort(int arr[],int low,int high)</span><br><span class="line">&#123;</span><br><span class="line">    int i=low;</span><br><span class="line">    int j=high;</span><br><span class="line">    int key=arr[low];</span><br><span class="line">    if(i&gt;j)</span><br><span class="line">    &#123;</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line">    while(i&lt;j)</span><br><span class="line">    &#123;</span><br><span class="line">        while(i&lt;j &amp;&amp; arr[j]&gt;=key)</span><br><span class="line">        &#123;</span><br><span class="line">            j--;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        while(i&lt;j &amp;&amp; arr[i]&lt;=key)</span><br><span class="line">        &#123;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line">        if(i&lt;j)</span><br><span class="line">        &#123;</span><br><span class="line">            swap(&amp;arr[i],&amp;arr[j]);</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">    swap(&amp;arr[low],&amp;arr[i]);</span><br><span class="line">    sort(arr,low,i-1);</span><br><span class="line">    sort(arr,i+1,high);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void swap(int* a,int* b)</span><br><span class="line">&#123;</span><br><span class="line">    int tmp=*a;</span><br><span class="line">    *a=*b;</span><br><span class="line">    *b=tmp;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">void your_dedup(struct result* res)</span><br><span class="line">&#123;</span><br><span class="line">    if(res-&gt;size&lt;=1)</span><br><span class="line">    &#123;</span><br><span class="line">        return;</span><br><span class="line">    &#125;</span><br><span class="line">    int new_size=1;</span><br><span class="line">    int i;</span><br><span class="line"></span><br><span class="line">    for(i=1;i&lt;res-&gt;size;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        if(res-&gt;arr[i]!=res-&gt;arr[i-1])</span><br><span class="line">        &#123;</span><br><span class="line">            res-&gt;arr[new_size]=res-&gt;arr[i];</span><br><span class="line">            new_size++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    res-&gt;arr=(int*)realloc(res-&gt;arr,new_size*sizeof(int));</span><br><span class="line">    res-&gt;size=new_size;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>输出结果：<br> <code>6 1 2 1 9 1 3 2 6 2 4 2 2 1 6 2</code><br> <code>1 1 1 1 2 2 2 2 2 2 3 4 6 6 6 9</code><br> <code>1 2 3 4 6 9</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>首先定义结构体，其中结构体有<code>int</code>类型的指针，和我们将要进行动态操作的数组。</li>
<li>在拼接函数内，首先为结构体内指针分配内存，用两个for循环，将两个数组依次输入到我们刚刚分配内存的指针内。我们之后的操作也是基于这个数组。记得把大小也赋值给结构体内，保存长度的<code>size</code>变量。</li>
<li>在排序函数内，我使用的是快速排序。</li>
<li>在去重函数内，我对数组进行for循环遍历，由于该数组已经排序完毕，所以可以依次比较。最后将去重后的数组重新分配内存，并且重新赋值数组大小。</li>
<li>记得free内存。</li>
</ol>
<ul>
<li>考点：<br> 结构体、排序算法、动态分配内存</li>
</ul>
<h2 id="11-指针魔法"><a href="#11-指针魔法" class="headerlink" title="11. 指针魔法"></a>11. 指针魔法</h2><p>用你智慧的眼睛，透过这指针魔法的表象，看清其本质：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">void magic(int(*pa)[6], int** pp) &#123;</span><br><span class="line">    **pp += (*pa)[2];</span><br><span class="line">    *pp = (*pa) + 5;</span><br><span class="line">    **pp -= (*pa)[0];</span><br><span class="line">    *pp = (*pa) + ((*(*pa + 3) &amp; 1) ? 3 : 1);</span><br><span class="line">    *(*pp) += *(*pp - 1);</span><br><span class="line">    *pp = (*pa) + 2;</span><br><span class="line">&#125;</span><br><span class="line">int main() &#123;</span><br><span class="line">    int a[6] = &#123; 2, 4, 6, 8, 10, 12 &#125;;</span><br><span class="line">    int* p = a + 1,** pp = &amp;p;</span><br><span class="line">    magic(&amp;a, pp);</span><br><span class="line">    printf(&quot;%d %d\n%d %d %d\n%d %d\n&quot;,*p,**pp,a[1],a[2],a[3],a[5],p-a);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br><code>6 6</code><br><code>12 6 8</code><br><code>10 2</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>在main函数内，先分配了一个数组，并且定义了一级指针<code>p</code>，和二级指针<code>pp</code>。此时p内为<code>a[1]</code>的地址，<code>pp</code>为<code>a[1]</code>的地址的地址。接下来我们在<code>magic</code>函数内逐步分析。</li>
<li>首先<code>magic</code>函数内传入了一个指向6个元素数组的指针的参数，和一个二级指针的参数。</li>
<li>第一行，<code>a[2]</code>的值和<code>a[1]</code>的值相加，赋给了<code>pp</code>。此时数组状态为<code>{ 2, 10, 6, 8, 10, 12 }</code>。</li>
<li>第二行，把<code>a[5]</code>的地址赋给了<code>pp</code>。此时数组状态为<code>{ 2, 10, 6, 8, 10, 12 }</code>。</li>
<li>第三行，把<code>a[5]</code>的值和<code>a[0]</code>的值相减，赋给了<code>a[5]</code>。此时数组状态为<code>{ 2, 10, 6, 8, 10, 10 }</code>。</li>
<li>第四行，先在内层进行判断，如果<code>a[3]</code>的值和<code>1</code>位与之后，结果非0，则把<code>1</code>作为参数，执行外层的代码。最终结果是把<code>a[1]</code>的地址赋值给了<code>pp</code>。此时数组状态为<code>{ 2, 10, 6, 8, 10, 10 }</code>。</li>
<li>第五行，把<code>a[1]</code>和<code>a[0]</code>的值相加，赋给了<code>a[1]</code>。此时数组状态为<code>{ 2, 12, 6, 8, 10, 10 }</code>。</li>
<li>第六行，把<code>a[2]</code>的地址赋给了<code>pp</code>。此时数组状态为<code>{ 2, 12, 6, 8, 10, 10 }</code>。</li>
<li>最后printf函数输出时，<code>*p</code>和<code>**pp</code>为<code>6</code>，<code>p-a</code>为<code>2</code>，即元素位置之差。其他对应输出相应数组元素即可。</li>
</ol>
<ul>
<li>考点：<br> 数组指针、二级指针</li>
</ul>
<h2 id="12-奇怪的循环"><a href="#12-奇怪的循环" class="headerlink" title="12. 奇怪的循环"></a>12. 奇怪的循环</h2><p>你能看明白这个程序怎样运行吗？试着理解这个程序吧！</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">union data &#123;</span><br><span class="line">    void**** p;</span><br><span class="line">    char arr[20];</span><br><span class="line">&#125;;</span><br><span class="line">typedef struct node &#123;</span><br><span class="line">    int a;</span><br><span class="line">    union data b;</span><br><span class="line">    void (*use)(struct node* n);</span><br><span class="line">    char string[0];</span><br><span class="line">&#125; Node;</span><br><span class="line">void func2(Node* node);</span><br><span class="line"></span><br><span class="line">void func1(Node* node) &#123;</span><br><span class="line">    node-&gt;use = func2;</span><br><span class="line">    printf(&quot;%s\n&quot;, node-&gt;string);</span><br><span class="line">&#125;</span><br><span class="line">void func2(Node* node) &#123;</span><br><span class="line">    node-&gt;use = func1;</span><br><span class="line">    printf(&quot;%d\n&quot;, ++(node-&gt;a));</span><br><span class="line">&#125;</span><br><span class="line">int main() &#123;</span><br><span class="line">    const char* s = &quot;Your journey begins here!&quot;;</span><br><span class="line">    Node* P = (Node*)malloc(sizeof(Node) + (strlen(s) + 1) * sizeof(char));</span><br><span class="line">    strcpy(P-&gt;string, s);</span><br><span class="line">    P-&gt;use = func1;</span><br><span class="line">    P-&gt;a = sizeof(Node) * 50 + sizeof(union data);</span><br><span class="line">    while (P-&gt;a &lt; 2028) &#123;</span><br><span class="line">        P-&gt;use(P);</span><br><span class="line">    &#125;</span><br><span class="line">    free(P);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>题解：</strong></p>
<ul>
<li><p>输出结果：<br> <code>Your journey begins here!</code><br><code>2025</code><br><code>Your journey begins here!</code><br><code>2026</code><br><code>Your journey begins here!</code><br><code>2027</code><br><code>Your journey begins here!</code><br><code>2028</code>  </p>
</li>
<li><p>解释：</p>
</li>
</ul>
<ol>
<li>首先在计算<code>sizeof(Node)</code>时，要注意结构体和共用体的内存对齐，对于共用体来说，内存共用且为最长字节类型的最小整数倍，即<code>24</code>。对结构体来说，每个变量对齐时，其对齐位置为上一次对齐后的下一位，为最长字节类型的倍数，总内存为最小整数倍，即<code>40</code>。</li>
<li><code>use</code>函数指针指向了<code>func1</code>，在进入while循环内，伴随着每一次条件的判断，在<code>func1</code>和<code>func2</code>来回交替执行，直到条件不成立。</li>
</ol>
<ul>
<li>考点：<br> 结构体和共用体内存对齐、函数指针</li>
</ul>
<h2 id="13-GNU-Linux-选做"><a href="#13-GNU-Linux-选做" class="headerlink" title="13. GNU&#x2F;Linux (选做)"></a>13. GNU&#x2F;Linux (选做)</h2><p>注：嘿！你或许对 Linux 命令不是很熟悉，甚至你没听说过 Linux。但别担心，这是选做题，了解 Linux 是加分项，但不了解也不扣分哦！</p>
<ol>
<li>你知道 cd 命令的用法与 &#x2F; ~ - 这些符号的含义吗？</li>
<li>你知道 Linux 系统如何创建和删除一个目录吗？</li>
<li>请问你还懂得哪些与 GNU&#x2F;Linux 相关的知识呢？</li>
</ol>
<p><strong>题解：</strong></p>
<ul>
<li>输出结果：</li>
</ul>
<ol>
<li><code>cd</code>:更改目录<br> <code>cd /</code>:切换至根目录<br> <code>cd ~</code>:切换至用户主目录<br> <code>cd -</code>:切换至上一级目录</li>
<li><code>mkdir 目录名称 </code><br> <code>rmdir 目录名称</code></li>
<li><code>ls</code>:列出文件<br> <code>touch</code>:创建文件<br> <code>clear</code>:清除终端页面<br> ……<br>D</li>
</ol>
]]></content>
      <tags>
        <tag>Linux</tag>
        <tag>C</tag>
      </tags>
  </entry>
</search>
